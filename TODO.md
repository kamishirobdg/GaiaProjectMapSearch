# TODO

タスク状態の正本。完了したらこのファイルから消し、経緯はコミットメッセージに残す。

## リリース状況

- 2026-07-30: `release/v1.01` を `main` へ FF push（89コミット、`719fd5e..3c4722f`）。
- **2026-08-02: 本番へ出した（`3c4722f..85e2e90`、69コミット）**。マーカーのセル単位化・
  Setup/List の評価表示と条件プロファイル・評価指数の再配分・タイル指定・標準技術の
  base/LF 分割・ラウンド得点のタイミング・研究トラックのレイアウト切替まで。
  **実データでの動作確認は全項目クリア**（List の「Mapで開く」／セットアップ→マップ／
  基準4「優位」／マップ候補の重複／一括探索の積み上がり／基本版で LF4種族を出さない）。
- **Vercel のビルド完了は HTML の Etag では判定できない**。SSR の出力が同じだと
  6分待っても Etag が変わらないのに、バンドルは差し替わっている。判定するときは
  ブラウザで実際の DOM を見ること（例: 研究トラックのセルの `align-self`）。
- 次も `git push origin release/v1.01:main`（origin/main は release/v1.01 の祖先なので
  FF で通る）。push＝Vercel本番デプロイ。未pushの件数は
  `git rev-list --count origin/main..release/v1.01` で数える（ここに数字を書くと
  毎コミットで古くなるのでやめた）。

## 要フィードバック（実データでの確認待ち）

**ここは値と見た目のチューニング**。直しても壊れないので、デプロイ後に順次で構わない。
2026-08-02 の実機確認で **列ラベル(RB/R点)・LF4種族の行背景・マーカーの視認性は
「問題なし」** と判定が付いたので、その3件はこのリストから消してある。

- **標準技術の評価値を拡張ごとに分けた**（2026-08-01、ユーザー指示）。拡張の有無で
  場に出るタイルの母集団が変わり、同じタイルでも相対的な影響力が変わるため。
  - `TECH_POSITION_WEIGHTS_BASE`（通常版・基本14種族）＝**ユーザー作成の CSV を反映済み**。
    `TECH_POSITION_WEIGHTS_LF`（拡張版・18種族）＝**2026-08-01 の値のまま。差し替え待ち**。
  - 参照は `techPositionCell(tileId, pos, lostFleet)` を通す。
  - 上級技術など他のタイルは当面共通。ただし**拡張ごとに上書きする仕組みだけ用意した**
    （`TILE_FACTION_WEIGHTS_LF` に書けばそのタイルだけ差し替わる。いまは空）。
  - CSV の反映と検算は `python scripts/gen_tech_position_table.py <csv> [--check]`。
    通常版は756セル全一致を確認済み（両方向）。
  - 通常版の実測（`_probe_faction_totals.ts --base`）: 最大イタル人114.3 /
    最小バルタック人74.2 = **1.54倍**（拡張版の5.3倍に比べてかなり均等）。
    影響力の順も 技術 > 上級 > ブースター > ラウンド > 最終 > 同盟 で狙いどおり。
    盤面ごとの最高値は中央値132 / 最大160、小数は0件。
  - **拡張版（LF）の CSV 作成はユーザー作業として残っている**（2026-08-02 確認）。
    桁は通常版の中央値132に対し拡張版128でほぼ揃っているので、`STD_TECH_SCALE` を
    拡張ごとに分ける必要はいまのところ無い。

- **Setup の評価値を18種族×全タイル見直した**（2026-08-01。「次にやること2」を実施）。
  根拠はルールブックの実文にそろえた: 基本14種族＝基本版 p20-21「別表Ⅰ：勢力」、
  LF4種族＝LF p13「別表Ⅰ：新たな勢力」、原始惑星／小惑星の入植コスト＝LF p10。
  **影響力は狙いの順になった**（4人LF 300件の種族間SD。`scripts/_probe_category_influence.ts`）:
  技術22.2 > LF船19.7 > 上級17.8 > ブースター14.9 > ラウンド13.5 > 最終9.7 >
  追加上級6.9 > 同盟3.6（係数調整前は 技術が6位・ラウンドが2位だった）。
  **この数字は基準10のときのもの**。基準を5へ下げ、ラウンド得点を表へ移したあとに
  再測すると 技術15.6 > LF船9.8 > 上級8.9 > ブースター7.5 > ラウンド6.6 >
  最終4.9 > 追加上級3.5 > 同盟1.8（2026-08-03）。**順序も相対比も変わらない**
  ―― 技術以外はきれいに半分で、基準の変更ぶんそのもの。

  **仕組みの整理**: タイルの重みは「そのタイルとその種族の噛み合い」だけを表し、
  「そのカテゴリの1枚がゲーム全体にどれだけ効くか」は**評価指数の既定値**で表す、と
  役割を分けた。既定値が基準から外れるのは2つだけ ——
  standardTech 6（基準の1.2倍。9枚が必ず出て最後まで残る）、
  roundScoring 4（基準の0.8倍。効くのは6ラウンドのうち1つだけ）。

  **ルールブックで判明して直した主なもの**（値を見るときの手がかり）:
  - ランティダ人の「他プレイヤーの惑星に置く鉱山」は**惑星の種類にもガイア惑星の数にも
    数えない**。種類系のタイル（AT15/FS03/RB12/RS11/TS2/ART07）から外した。
    **除外はこの「数を数える」系だけ**（基本版 p20「別表Ⅰ：勢力」に
    「他のすべての点においては通常の鉱山と見なされます」と明記されている）。
    **2026-08-02 訂正**: TS7（ガイア惑星に鉱山を建設するたびに＋3VP、p8）は
    「数える」系ではなく「建設したときのトリガー」系なので除外対象外 —
    ランティダ人が他プレイヤーの入植済みガイア惑星に鉱山を置いても発動する。
    以前はこれもTS2などと同じ扱いで除外してしまい、
    `TECH_POSITION_WEIGHTS_BASE.TS7` の lantids が全列0のままになっている
    （CSV反映時に引き継がれた誤り）。AT14（同じ「鉱山を建設するたび＋3VP」効果、
    lantids:2）と平仄を合わせて、次のユーザーの値レビューで直す。
  - バル・タック人は**航法を進められない**。深宇宙・宙域系（AT17/AT21/FS05/FS09/
    RB13/RS10/ART06）を全部マイナスにした。以前は1つも入っていなかった。
  - スペースジャイアントの「無料2段階改造＋鉱山建設」は**ラウンドごとに1回**（p10）。
    通常惑星が常に2段階なので毎ラウンド無料で鉱山が増える＝鉱山数・惑星種類・
    改造段階の主役に引き上げた。惑星首府の技術タイルは逆に**1回だけ**。
  - ティンカーロイドの RB09/AT16 は「**建設済みの**首府・学院1つごと」の数え上げなので、
    初期配置の首府が最初から乗る（＋）。RS04 だけが「建設したとき」の誘発で取り逃す（−）。
    以前は RB09 が −1 で AT16 が +2 と矛盾していた。
  - グリーン人は**QICを得られない**。QIC付きのタイル（TS1/RB02/FED8Q/FEDG5/ART09）を
    マイナスに、AT20（QICアクションのたび+4VP）は −2 にした。
  - フィラク族の「研究所→交易所の格下げ」は**「交易所への改良」と見なされる**ので、
    交易所を建てるたび系（AT11/RS02/RS03/RB08/AT09）が毎ラウンド誘発する。
  - ダルカニア人（通常惑星が全色1段階）と RB04（無料1段階）はコストがちょうど一致。
    ジオデン人のPIと RS11、ダルカニア人のPIと RS10 も誘発条件が完全一致。

  **2026-08-02 の判定**:
  1. **個々の値の見直しはユーザー作業**（拡張種族ぶん）。編集用テーブルは
     `src/gaia/eval/factionWeights.ts` の `TILE_FACTION_WEIGHTS`（86タイル）と
     `TECH_POSITION_WEIGHTS`（9タイル×7配置）。母星色ごとの平均は
     小惑星110 / 白89 / 原始84 / 橙75 / 黄72 / 茶72 / 青71 / 黒65 / **赤48**。
     正規化しない方針（案C）なので幅が出ること自体は設計どおり。
  2. **standardTech 6 / roundScoring 4 の既定値は現状のままで確定**（ユーザー確認済み）。
     一律5に戻すと 技術が3位・ラウンドが4位になるので、この差は残す。

  **付随して変えたもの**:
  - 評価値の桁: 見直しで非ゼロのセルが増えたぶん膨らんだので、**基準の係数を 10 → 5**
    に下げて元の狙い（100前後）に戻した。実測で盤面ごとの最高値が中央値112・最大179
    （`scripts/_probe_score_range.ts`）。小数は0件。
  - ラウンド得点は**タイル1枚ごとに整数へ丸める**ようにした。係数が10の倍数でないと
    「重み×倍率×係数/10」が小数になるため。丸めを1枚ごとにするのは、内訳表の合計と
    タイル強調の合計を必ず一致させるため（両方が同じ関数を通る）。
    副作用として、評価指数を2倍にしてもラウンド得点だけはぴったり2倍にならない
    （1枚あたり最大0.5、6枚で±3のズレ）。
  - Setup の色優遇の係数を **0.25 → 0.3**（`measure_setup_color_pref.ts` を再実行。
    掛け先の振れ幅が 53.1 → 37.7 に縮んだため）。目盛りの文言は従来どおり。
  - **保存データ**: IndexedDB v10 / localStorage マーカー v4。**今回は Map 側の重みを
    触っていないので、消えるのは Setup/List 側だけ**（条件プロファイル・一括探索の
    ランキング・List の提案とログ）。**Map のピン留め・候補・条件は残る**。
    Setup の保存リストも残る（スコアは表示のたびに計算し直すので新しい値で出る）。

- **Setup の評価をタイル配置ベースへ作り直した**（2026-07-31）。
  - **標準技術**: 「トラック下6枚」と「フリー枠3枚」の2テーブル・2係数をやめ、
    `TECH_POSITION_WEIGHTS[タイル][配置][種族]` の1テーブル・1係数（standardTech）に
    統合。配置は研究列6つ＋"free" の7通り。評価指数の列も2つ→1つになった。
    **フリー枠の効き具合が上がっている**（専用の低い係数をやめたため。トラック比
    0.2 → 0.4 相当）。値の見直しは「1つずつ精査」で。
  - **ラウンド得点に並び順を反映**。`ROUND_SCORING_TIMING` が何ラウンド目に出たかで
    倍率を変える（10分率。どの曲線も6ラウンドの合計が60＝平均10なので、全体の
    スケールは従来と同じ）。**曲線は 2026-08-02 に精査して後半寄りへ倒した**
    （下の「決定済み」参照）。
  **2026-08-02 の判定**: ラウンド得点の評価値そのものは、下の「直近の依頼で未着手」の
  CSV 化でまとめて作り直す。曲線はそこで廃止するので、いまの値の追い込みはしない。

- **色優遇/冷遇を3タブへ広げた**（2026-07-31）。目盛りはどれも
  「0=効かない / 1=基準とほぼ互角 / 2=優遇が主導 / 5=実質それだけで決まる」で揃えてあり、
  文言は実測に基づく（`scripts/measure_color_pref.ts` /
  `scripts/measure_setup_color_pref.ts`。重みを変えたら再実行して文言を合わせ直すこと）。
  - **Map**: 原始・小惑星も対象に。掛け先は extraBest（最良の1惑星×補正値）。
    **wColorPref の既定を 3 → 25 に変えた**。3 のままでは pref=2 が偏り項の0.2倍しか
    効かず、実用域が10〜20だったため。既定の pref は0なので既定の検索結果は不変。
  - **Setup**: 母星色ごと。掛け先は「その色でいちばん強い種族のスコア」。
    係数 0.25（内部固定、入力欄には出さない）。一括探索の並びにだけ効く。
  - **List**: 種族ごと。掛け先は「Map の評価値（母星色ぶん）＋ Setup の評価値」の合計。
    係数は Setup の半分（掛け先が約2倍の大きさになるため）。
  **実データでの確認が未**: 目盛りの体感が文言と合っているか（特に pref=1 と 2 の差）。
  List は Map 候補の評価内訳から Map ぶんを引くので、内訳を持たない古い候補では
  Map ぶんが0になる（Setup ぶんだけで効く）。

- **評価値の桁をゲームの得点に合わせた**（2026-07-31）。色ごと／種族ごとの評価値が
  100前後、Map と Setup を足して200前後になる（実測: Map 最大101 / Setup 最大123）。
  小数はすべて消してある。**貯めてあった結果・条件は全部消えている**
  （IndexedDB v9 の upgrade と localStorage の一度きりの掃除。ユーザー確定済み）。
  要注意点が3つ:
  - **距離減衰 `DISTANCE_FALLOFF` は重みのスケールと連動する。** 船接触・船星系は
    「重み −（距離−1）×これ」で引く量が絶対値なので、重みだけ動かすと減衰の形が
    変わって評価の意味が変わる。**スケールを変えるときは必ず一緒に変えること。**
  - **星系の軸は色ごとの合計で丸めている。** 次元横断を0.5で数えるため小数が出る。
    クラスタ単位ではなく合計で丸めるので 0.5 が2つあれば打ち消し合うが、
    0.5 が1つだけ残る盤面では切り上がる（回帰スナップショットの base_34p 90件が
    最大8.94%動いた。LF 60件は不変）。
  - **Setup のフリー枠係数の比率が動いた**（基準の 1/4 → 1/5）。基準10では 2.5 が
    整数にならないため。「トラック分より軽い」意図は保っている。1/4 を厳密に保つには
    基準20（評価値が最大280前後）が必要で、100前後という狙いから外れる。
  平均・散らばり・一括探索の基準値は割り算なのでもともと小数。整数へ丸めている
  （この丸めの粒度が実用的かは要確認）。

- **原始・小惑星の「最良の1つ×補正値」**（2026-07-31 実装）。内訳表の追加行が
  種別ごとの単純合算から「4軸（船接触＋船星系＋ガイア＋星系）の合計がいちばん大きい
  1惑星 × 補正値」に変わった。行クリックのマーカーもその1惑星だけを光らせる。
  **補正値 2.75 は 2026-08-02 に調査して据え置きと結論**（下の「決定済み」参照）。
  検索スコア本体には入らないが、**色優遇を付けたときの Map スコアと、List の
  種族優遇の掛け先には効く**（当初「表示専用」と書いていたのは不正確だった）。
  既定は pref=0 なので、既定の検索結果・保存済みの結果・回帰スナップショットは不変。

- **Setup の一括探索＋ランキング**（2026-07-31 実装）。いまの条件（タイル指定・面の
  指定込み）で大量生成し、基準の良い順に上位20件を **IndexedDB に保存**する。
  次の探索は前回の上位とマージしてまた上位20件を残す（Map の candidates と同型）。
  基準はマップ非依存の2つ（上位バランス／全体バランス）をプルダウンで選び、
  結果は条件×基準ごとに分かれる。件数と基準は localStorage に保存。
  生成は 0.25ms/件 と軽いのでワーカーは使わず、250件ごとに setTimeout で UI を
  明け渡すチャンク実行にした（最大5000件、進捗と中止あり。中止してもそこまでの
  上位は保存される）。行クリックで表示、行の「記録」で保存リストへ入る。
  表示位置は種族別評価の下（ユーザー指定）。**2026-08-02 に実データで確認済み**。

- **LF4種族の重み DRAFT**（2026-07-31 投入、`factionWeights.ts`）。**値のレビュー待ち**。
  根拠は LF ルールブック（`web_GP_LostFleet_JPN.pdf`「別表Ⅰ：新たな勢力」13ページ）の
  能力のみ。アーキタイプは モウェイド人=パワー経済・同盟形成 /
  スペースジャイアント=惑星改造・惑星種類 / ティンカーロイド=初期首府の追加アクション /
  ダルカニア人=広域展開。
  **いちばん弱いのは研究列の親和度**（専用テーブルは無く、`TECH_POSITION_WEIGHTS` の
  各セルに溶け込んでいる）。勢力ボードの初期資源・
  初期研究レベルが PDF の画像ページで読めておらず、能力からの推定で仮置きしてある。
  実測（LF4人24盤面の平均・18種族中）: ダルカニア人1位12.11 / モウェイド人5位9.60 /
  スペースジャイアント7位9.01 / ティンカーロイド13位7.49。
  既往の帯（4.06〜11.56）にほぼ収まる範囲。正規化しない方針（案C）は維持。

- **評価指数の既定値を変更した**（2026-07-30）。船接触 6→10、ガイア距離2 3→8、
  距離3 1→3（他は据え置き）。各軸が全体に占める割合を24盤面で実測して、
  影響力が 船接触 > 船星系 > ガイア > 星系 > 最外周 > 外周 の順になるよう決めた。
  保存済みの検索条件プロファイルは起動時に自動適用されるため、そのままでは
  新しい既定値が出てこない。**「保存済み条件」パネルの「既定値で新規」**を押すと
  条件が既定へ戻り、自動適用のポインタも消える（保存済みのマップ・条件は消えない）。

## 残タスク

### 次にやること（2026-07-31 ユーザー指示。この順で）

1. ~~**原始惑星・小惑星の補正値が妥当かの調査**~~ → **2026-08-02 実施済み**。
   結論は **2.75 のまま据え置き**（下の「決定済み」参照）。

2. ~~**Setup の評価値を1つずつ精査する**~~ → **2026-08-01 実施済み**。
   上の「要フィードバック」を参照。影響力は狙いの順になった。残っているのは
   ユーザーによる値のレビューだけ。**まだ手を付けていない下位項目**:
   - ~~`ROUND_SCORING_TIMING` の曲線と割り当て~~ → **2026-08-02 に精査済み**
     （下の「決定済み」参照）。
   - 勢力ボードの**初期研究レベルが今も未確認**（ルールブックの別表になく、
     勢力ボードの画像にしかない）。研究列の親和度はそのぶん能力からの推定を含む。
     実物かボード画像が手に入れば `TECH_POSITION_WEIGHTS` の精度が上がる。

### 直近の依頼で未着手

（実装側は空。**ユーザー作業**として残っているものは「判断待ち」の項を参照）

- ~~**Setup の研究トラックを6列に収める**（案A）~~ → **2026-08-02 実装・実機確認まで完了**。
  研究トラックの見出し横のボタンで「横スクロール（既定）／画面幅に収める」を切り替え、
  選択は localStorage `gaia_setup_layout` に持つ。幅は CSS だけで決める（JS で画面幅を
  測らないので SSR とちらつかない）。
  - **6トラック×4段の grid** にしてある（`SetupView.tsx`）。列ごとの縦積みだと
    トラック名の行数（航行=1行 / ガイアプロジェクト=3行）と「指定」バッジの有無で
    段の位置がずれるため。トラック名と上部スロットは**下端**でそろえ、上級・標準の
    セルは `alignSelf: stretch` ＋ `display: grid` で枠を段の高さまで伸ばす。
  - タイルの枠と画像は CSS 変数（`--setup-tile-w` / `--setup-tile-max`）で列幅に追従。
    変数を置かない他の節は 110px のままで影響しない。枠には `box-sizing: border-box`
    が要る（`width:100%` に padding が加算されて 16px はみ出していた）。
  - 実測: 本番 375px 幅で列52px・画像35px（実機393pxなら列55px前後）。
    段の下端は全列一致。

- **表示まわり3件を実装した**（2026-08-02。**実データでの確認待ち**）。
  1. **List のセット提案プレビューを Setup にそろえた**（`SetupBoard`）。見出しが
     free tech / scoring / boosters / LF ships と英語のままだったので UI 辞書の
     日本語ラベルへ差し替え、節の順番も本体に合わせた（フリー枠 → ラウンド得点 →
     最終得点 → 得点ボード拡張部 → LF船 → アーティファクト → ブースター）。
     LF船は船ごとに縦へまとめ、船名を出して「金枠同盟 → 基本技術」の順にした
     （以前は追加上級・各船のタイル・アーティファクトを1行に流していた）。
     **埋め込みブラウザではセット提案を出せない**（保存データが無い）ので実機確認が要る。
  2. **研究トラックの「上級」「標準」タグを省いた**。"fit" だけでなく常に省く。
     上級／標準は絵柄と段の位置で分かるため。ホバーの tooltip には残してある。
  3. **「指定」バッジをタイル画像の右上へ重ねた**。枠は `position: relative`。
     実測（幅1280）で上級の段は6列とも 101px・下端一致、標準の段は 97px・下端一致。
  - **狭い画面（560px以下）ではバッジを緑の丸にした**（2026-08-02 ユーザー確定・案B）。
    24x14px のままだと本番375pxの列52px（画像35px）で画像幅の約7割を覆うため、
    8px の丸（23%）に落とす。`.setup-pin-badge` の `@media`（`SetupView` の `<style>`）。
    閾値は fit で列が 80px を切るあたり＝スマホだけが対象になる幅。
    **実機での見え方は未確認**（埋め込みブラウザでは狭い幅を再現できない）。

- **ラウンド得点を「タイル×ラウンド×種族」の表にした**（2026-08-02 実装。**値の入力待ち**）。
  - `ROUND_SCORING_WEIGHTS_BASE`（9×6×14＝756セル）/ `_LF`（12×6×18＝1296セル）。
    参照は `roundScoringCell(tileId, roundIndex, lostFleet)`。
    **曲線 `ROUND_SCORING_TIMING` は廃止**（`roundTimingOf` / `ROUND_TIMING_BASE` /
    `roundScoringContrib` の丸めも消えた。係数は他カテゴリと同じ一括スケール）。
  - **いまの中身は雛形**（案1＝`TILE_FACTION_WEIGHTS` の RS 行を全ラウンドへ複製）。
    **ラウンド差はゼロの状態**で、以前の序盤型/中盤型/終盤型の差はいったん消えている。
    ここに値を入れるのがユーザー作業。
  - `TILE_FACTION_WEIGHTS` の RS 行は**もう評価に使われない**（雛形生成の入力と記録
    として残置）。直しても評価は変わらないので、直すのは新しい表のほう。
  - 生成・検算は `python scripts/gen_round_scoring_table.py`:
    `--template [--lf]` で雛形CSV（`対応表/タイル/ラウンド/種族名`）、
    引数にCSVを渡すと TS ブロック、`--check` で全セル突き合わせ（両方向）。
    雛形との突き合わせは通常版210・拡張版384セル（非ゼロぶん）で全一致を確認済み。
  - 影響（`_probe_category_influence.ts`、4人LF 300件）: ラウンドの種族間SDは
    6.7 → 6.6、占有率 11.4% → 11.2% でほぼ不変。盤面間SDだけ 5.2 → 4.9
    （並び順による盤面ごとの差が消えたぶん）。他7カテゴリは完全に同値。
  - **保存データは触っていない**（IndexedDB のバージョンは据え置き）。評価値は表示の
    たびに計算し直すので保存リストは新しい値で出るが、**一括探索の保存済みランキングは
    古い基準値のまま並ぶ**。気になるなら「消去」で作り直す。

- **上級技術を「タイル×研究列×種族」の表にした**（2026-08-03 実装。**値の入力待ち**）。
  - `ADVANCED_TECH_WEIGHTS_BASE`（15×6×14＝1260セル）/ `_LF`（21×6×18＝2268セル）。
    参照は `advancedTechCell(tileId, track, lostFleet)`。中身が大きいので
    `src/gaia/eval/advancedTechWeights.ts` へ分離し、**CSV から丸ごと自動生成**する。
    **このファイルは手で編集しない**（CSV を直して生成し直す）。
  - **値は VP 換算**（ユーザー確定）。「その上級技術を適切なタイミングで取れたら
    何点分の価値があるか」。20〜30 程度が上限、0 はほぼ無く 5 以下も稀。
  - **いまの中身は雛形**: 既存の相性値（`TILE_FACTION_WEIGHTS` の AT 行、-2..+2）を
    `24 + 3×v` で 18〜30 へ写像し、6列すべてへ複製した状態＝**列ごとの差はゼロ**。
    「この列に置かれるとこの種族は取りに行けない」を入れるのがユーザー作業。
  - **得点ボード拡張部の1枚は6列の最大値**（`advancedTechExtensionCell`）。研究列に
    紐付かず、どの列を登っていても取りに行けるため（標準技術のフリー枠と同じ理屈）。
    取得条件そのものの違いは係数 advExtension 側で見る。
  - 生成・検算:
    雛形 `python scripts/gen_advanced_tech_table.py --template [--lf] > out.csv`
    反映 `python scripts/gen_advanced_tech_table.py --emit-file src/gaia/eval/advancedTechWeights.ts base.csv lf.csv`
    検算 `python scripts/gen_advanced_tech_table.py <csv> --check`
  - **係数を 5 → 2**（`ADVANCED_TECH_SCALE`）。VP 換算で桁が上がり、5 のままだと
    種族間SD 26.7・占有率32%で単独1位になる。2 なら 10.7 で技術15.6 に次ぐ位置。
    **値が固まったら測り直す暫定値**。
  - `TILE_FACTION_WEIGHTS` の AT 行は**もう評価に使われない**（雛形生成の入力と記録
    として残置。ラウンド得点の RS 行と同じ扱い）。

- **全カテゴリを VP 換算にした**（2026-08-03 ユーザー確定。**実装完了・値の入力待ち**）。
  「そのタイル1枚が何点分の価値を持つか」で全カテゴリの物差しを揃えた。表は4本に
  分かれ、**どれも CSV から丸ごと自動生成する**（該当の .ts は手で編集しない）。

  | 表 | ファイル | 生成スクリプト | 中央値 | 刻み | セル数(通常/拡張) |
  | --- | --- | --- | --- | --- | --- |
  | 上級技術 | `advancedTechWeights.ts` | `gen_advanced_tech_table.py` | 18 | 3 | 1260 / 2268 |
  | 標準技術 | `techPositionWeights.ts` | `gen_tech_position_table.py` | 12 | 2 | 756 / 972 |
  | ラウンド得点 | `roundScoringWeights.ts` | `gen_round_scoring_table.py` | 10 | 2 | 756 / 1296 |
  | ブースター/最終/同盟/LF船 | `tileWeights.ts` | `gen_tile_weights_table.py` | 6/9/8/10 | 2 | 308 / 954 |

  **CSV の正本は `data/weights/`**（2026-08-04 に配置。手順は同ディレクトリの
  README.md）。`<表>_base.csv` / `<表>_lf.csv` の8本で、生成先の .ts は
  自動生成物なので手で編集しない。どのスクリプトも同じ使い方:
  `--template [--lf]` で雛形CSV / `--emit-file <out.ts> <base.csv> <lf.csv>` で反映 /
  `<csv> --check` で全セル突き合わせ（両方向）。
  - **いまの中身はどれも雛形**（2026-08-04 に**タイルごとの素点**へ作り直した。
    下の「素点の入れ方」参照）。列やラウンドの差はまだゼロで、そこを入れるのが
    ユーザー作業。
  - 係数は**桁合わせだけの役割**になった。既定は 基準9 / 標準技術9 / 上級6 /
    ラウンド6 / 追加上級2（追加上級だけ通常の上級の 1/3）。
  - **影響力の順は目安**（2026-08-04 ユーザー判断）。実測は
    技術 > LF船 > 上級 > 最終 > ブースター > ラウンド > 同盟 > 追加上級 で、
    表示順（＝評価表の列の並び）とは最終得点の位置が違う。**一致させるために
    係数を微調整することはしない** —— 最終得点は全員の順位で大きく差が付くので
    4〜6位のどこに来ても違和感はない、というのがユーザーの評価。
  - 色優遇の係数（`SETUP_COLOR_PREF_W`）は再測して **0.3 のまま据え置き**。
    掛け先の振れ幅は 37.7 → 45.8 に増えたが、基準値の振れ幅も一緒に増えたため。
  - `TILE_FACTION_WEIGHTS`（`factionWeights.ts`）は**もう評価に使われない**。
    4本の雛形生成の入力と、「VP 換算前はどの相性値だったか」の記録として残置。

  **2026-08-04 の仕上げ**（ユーザーの実プレイ感覚に合わせた3点）:
  - **上級技術の中央値を 24 → 18**。取得に同盟タイル1枚を払う対価を引くと、
    標準技術（12）の1.5倍あたりが妥当。実際に取る2枚ぶんで 53.3 → 41.3 点。
  - **追加上級は通常の上級の 1/3**（`ADV_EXTENSION_SCALE`）。拡張部の1枚を
    3〜4人で奪い合うため。整数比を保つため係数体系を3倍にした
    （基準9 / 標準技術9 / 上級6 / ラウンド6 / 追加上級2）。
  - **最終スケール `SETUP_SCORE_DIVISOR = 40`**。VP のまま合算すると3540〜4284に
    なるので、最後に割って **88.5〜107.1（中央値101）** へ落とし、Map 側の
    評価値（100前後）と同じ土俵に乗せる。桁を揃えるためだけのもの。
    - 割り算で**評価値に小数が出る**。整数へ丸めると1枚しか出ないカテゴリ
      （追加上級・同盟）で種族差が消えるので、表示は小数第1位まで（`fmt1`）。
      内訳と合計は割る前の値で一致するので、表の縦計は合う。
  - 実プレイの取得枚数（標準技術7 / 上級2 / ラウンド2 / ブースター6 / LF船3 /
    追加上級1）で数え直すと **209.8 点**（`_probe_expected_score.ts`。VP そのまま）。

  **素点の入れ方**（2026-08-04）。全タイル共通の中央値だと、相性値0のセル（＝大半）が
  同じ値に張り付いて「取れたら何点か」が入らない（実測で上級技術の 72% が18点、
  タイル別の平均も 18.4〜19.5 でほぼ差なしだった）。そこで各スクリプトに
  **`TILE_VP`（タイルごとの素点）** を持たせ、種族差は**素点への比例**
  （相性1につき25%＝`TEMPLATE_RATIO`）で乗せる形にした。
  - 素点は**別モデル（Fable）にゼロから独立に見積もらせ**、こちらの値と突き合わせて
    確定した。見せて直させると追認になりやすいので値は渡していない。
  - 突き合わせで分かったこと: **「〜するたび」系はこちらが一貫して楽観的**だった。
    上級技術は R4 前後に取るので残りは3ラウンドしかなく、取得後に起きる回数は
    思ったより少ない（AT02 研究を進めるたび+2VP は 16 → 8）。取得時・パス時系は
    ほぼ一致し、AT04/AT09/AT10/AT17 は完全一致だった。
  - 標準技術だけは 2026-08-01 に入れた列ごとの実データがあるので、いまの値から
    相性値を逆算してから新しい素点へ掛け直している（`LEGACY_BASE` / `LEGACY_STEP`）。
    **CSV に値を入れ始めたら `--template` は使わない**（この逆算が成り立たなくなる）。

  **素点の見直し**（2026-08-06 ユーザー指示。4点まとめて反映した）:
  - **TS1（即時：鉱石1＋QIC1）3 → 11**。収入系の TS5（鉱石1＋パワー1）に勝る ——
    R1 に取って鉱石1を鉱山に回せばその鉱山が以降5ラウンド収入を生み、QIC は
    技術タイル獲得にも入植（ガイア・距離）にも使えて選択の幅が広い。
    平均 3.7 → 13.5（TS5 は 12.0）。
  - **TS3（首府・学院のパワー値4）4 → 12。拡張版は 14**。きちんと機能させれば
    同盟を1つ増やせるので同盟1枚ぶんの価値がある。拡張版は金枠同盟と LF船のぶん
    さらに大きい。平均 4.3 → 12.9（拡張版 4.5 → 15.7）。
  - **TS9（アクション：パワー4）7 → 12**。獲得ラウンドから効き、しかも自分に都合の
    良いタイミングで使えるので収入系に勝る。平均 9.8 → 16.8（収入系の最大は TS6 の
    14.5）。タクロン族・ネヴラ人が最大というのは列ごとの差に既に入っていて、
    比例スケールなので保たれる。
  - **上級技術は全タイル +4**（中央値 18 → 22）。どの研究列でも進められるぶん、
    トラック下でしかマーカーを進められない標準技術より最大4点高い、という評価。
  - 反映は **`scripts/rescale_weight_csv.py`（新規）**。素点だけを差し替えて、
    CSV に入っている列ごと・種族ごとの差は**比例で保つ**
    （値 = 素点 × (1 + 0.25×相性) なので、新旧素点の比を掛ければ相性は不変）。
    `--template` は 2026-08-03 の雛形の写像から相性を逆算する作りで、素点ベースへ
    移した今の CSV には使えないため、この道具が要る。
  - 影響力（`_probe_category_influence.ts` 4人LF 200件の占有率）:
    技術 25.9% → **33.4%**、上級 16.7% → **18.9%**。他は絶対値が変わらず
    相対的に下がっただけ。順は 技術 > 上級 > LF船 > 最終 > ブースター > ラウンド >
    同盟 > 追加上級 で、**上級と LF船 が入れ替わった**。
    係数では合わせ込まない方針（2026-08-04）のままなので調整はしていない。
  - 桁: 種族スコア 93.2〜125.2、盤面ごとの最高値の中央値 113.9。実プレイの取得枚数
    ぶんでは 244.8 点（`_probe_expected_score.ts`。2026-08-04 時点は 209.8）。

- **種族別総合評価タブを作った**（2026-08-04 実装。**実データでの確認待ち**）。
  4つ目のタブ `/total`（`TotalView.tsx`）。マップ1枚とセットアップ1つを選び、
  種族ごとに「Map の評価値 ＋ Setup の評価値」を足して合計の降順で並べる。
  - Map ぶんは `mapValueByFaction(候補の evaluation.breakdown)`（母星色の評価値＝
    Map タブの内訳表と同じ）。Setup ぶんは `scoreSetupFactions`（Setup / List と同じ）。
    足せるのは Setup 側を VP 換算＋最終スケールで100前後に揃えたため。
  - 選択肢はマップが `buildMapPool`（ピン留め→ランキング）、セットアップが保存リスト
    （ピン留め→新しい順）。**評価内訳を持たない古い候補は Map ぶんが0**になるので、
    その場合は画面に注意書きを出す。
  - **埋め込みブラウザでは空状態しか確認できない**（保存データが無い）。表が正しく
    出るかは実機で見る必要がある。

- **重み CSV をスマホから編集するページを作った**（2026-08-06 実装）。`/weights`
  （`WeightsEditor.tsx`）。CSV への値入力を Android から進めるための道具なので、
  評価ツール本体ではない。**タブには出していない**（URL を直接開く）。
  - 初期値はアプリに焼き込まれた4本の表なので、CSV の貼り付けは要らない。
    **CSV も生成物も書き換えない** —— 変わったセルだけを差分テキストで出し、
    `python scripts/apply_weight_edits.py edits.txt` で CSV へ戻す
    （`--dry-run` あり。未知のタイル・軸・種族があれば何も書かずに止まる）。
  - 入力は2モード。**一括**＝種族 × 研究列（またはラウンド）の倍率で、その表の
    全タイルに効く（「バルタック人は航法を進められない」は1回で済む）。
    **タイル**＝タイル1枚ごとの基準値と列ごとの上書きで、一括より優先。
    倍率は 100/75/50/25/0%。タイルモードにはルール全文（`data.ts` の `effect`）を出す。
  - 基準値の既定は**現在の表の軸横断の最大値**。列差が入った表を読み直しても
    基準値が目減りしないようにしてある（「取れたら何点か」の定義に合わせる）。
  - 375px 幅で横スクロールが出ないことを確認済み（行見出し68px＋セル44px）。
    編集は localStorage `gaia_weight_edits` に溜まる。反映が済んだら「全消去」を押す
    （生成し直した TS が次の初期値になるので、同じ差分を二度出さないため）。
  - 手順は `data/weights/README.md` の「スマホから編集する」。

- **Map と Setup のバランスを決める**（2026-08-04 ユーザー指示で保留。**検討中**）。
  総合評価タブの合算は**いまは 1:1 固定**。単純な 1:1 を既定にするより良い配分が
  ありそう、というのがユーザーの見立て。決まったら倍率として入れる。
  - 材料になりそうな観点: マップは全プレイヤーが同じ盤面を共有するのに対し、
    セットアップのタイルは早い者勝ちで奪い合う／マップの評価値は母星色ごとなので
    同じ色の2種族で同点になる（＝種族の差が出るのは Setup 側だけ）。
  - 入れ先は総合評価タブの列の重み。決めたら `factionPrefBonus`（種族優遇の掛け先）
    にも同じ倍率を通す。

### 判断待ち（着手前にユーザーへ）

- **評価値の桁をどうするか**（2026-08-03。VP 換算の副作用）。種族あたりの評価値が
  **1288〜1544**（`_probe_score_range.ts`、4人LF 300件）になった。従来の狙いは
  「100前後、Map と足して200前後」だったので大きく外れている。原因は
  **場に出ている全タイルを取れる前提で足している**こと —— 実際に取れるのは一部
  （上級技術なら1〜2枚）なのに、6枚ぶんの点数を足している。
  - 案A: そのまま。順位付けは相対値なので実害は無い。数字がゲームの得点に見えなくなる。
  - 案B: 係数をさらに下げて100前後へ戻す。ただし整数のままだと粒度が粗くなる
    （基準3 → 1 でも 1/3 にしかならない）。
  - 案C: カテゴリごとに「実際に取れる枚数の期待値」で割る（上級技術なら 1/4 など）。
    理屈は正しいが、その期待値をどう決めるかという新しい判断が要る。
  - 案D: 内部は VP のまま、**表示だけ 1/10** にする。内訳表・合計・強調表示が
    同じ関数を通っているので、割り算を1箇所に入れれば整合は保てる。

- **旧ルール入力の後片付け**（低優先）。UI を全スロットのタイル指定へ統合したので、
  `BuildSetupInput` の `avoidRules` / `forceRules` / `forceTileRules` /
  `allowTileRules` / `shipDistanceAvoid` / `shipDistanceForce` / `rebellionGoldFed`
  は**もう誰も設定しない**（エンジンと共有リンクの復元だけが読む）。
  `AVOID_RULES` は既定の除外を作るためだけに残っている（`allowTiles` は未使用）。
  - **条件サマリは 2026-08-02 に対応済み**。旧フィールドは0件で行ごと出ないため
    タイル指定を入れても条件を見分けられなかったので、`countTileRules`
    （`src/gaia/setup/tileRules.ts`）で「固定N / 候補N / 除外N」を出すようにした。
    除外だけは既定との差を数える（既定の除外はどの条件にも同じだけ入っているため）。
  - **残っているのは型からフィールドを消すかどうか**。消すと古い共有リンクの復元が
    落ちるので、移行の要否とセットで判断する。
- **List のミニ盤面にはマーカーが出ない**: 評価表クリックのタイル点滅は Setup 専用。
  List の提案プレビュー（SetupBoard/SmallTile）は対象外のままにしてある。

- **スカウトの船別色**（要判断）: 現状マーカーは惑星色で統一。船ごとに色を割り当てるか、
  どの船由来かはポップアップだけで示すか。
- **種族別重みの値レビューはユーザー作業**（`src/gaia/eval/factionWeights.ts`）。
  正規化は行わない方針（案C）で確定。編集用テーブルは `TILE_FACTION_WEIGHTS`（86タイル）
  / `TECH_POSITION_WEIGHTS_BASE` / `TECH_POSITION_WEIGHTS_LF`
  / `ROUND_SCORING_WEIGHTS_BASE` / `ROUND_SCORING_WEIGHTS_LF`。
  カテゴリ単位の効き具合は画面の「評価指数」から（既定は standardTech 6 /
  roundScoring 4 / 他 5）。**2026-08-02 時点で残っているのは3つ**:
  - 拡張種族ぶんの更新（`TILE_FACTION_WEIGHTS` / `TECH_POSITION_WEIGHTS_LF`）
  - `TECH_POSITION_WEIGHTS_BASE.TS7` の lantids（全列0のまま。AT14 に合わせて 2 程度）
  - **ラウンド得点表のラウンド差**（`ROUND_SCORING_WEIGHTS_*`。いまは全ラウンド同値の
    雛形。CSV で編集するなら `scripts/gen_round_scoring_table.py`）
  こちらから触ると編集が衝突するので、依頼があるまで手を出さない。

## 検討事項（着手前にユーザー確認）

- ~~ラウンド得点のタイミングを種族ごとに設定する~~ → **CSV 方式で確定**。
  上の「直近の依頼で未着手」に一本化した（曲線に種族の例外を足すのではなく、
  タイル×ラウンド×種族の表を作って曲線ごと置き換える）。

- 3人時の 09/10 除外縮小盤 `base_3p_small`（要望があれば別テンプレートで）。
- ~~種族別総合評価タブ~~ → **2026-08-04 実装済み**（上の「直近の依頼で未着手」参照）。

### ルールと突き合わせて出た実装候補（2026-08-03 の棚卸し）

- ~~**上級技術も「どのトラックの下か」で評価する**~~ → **2026-08-03 実装済み**
  （上の「直近の依頼で未着手」を参照）。フル表・BASE/LF 別・VP 換算で確定。

- **LF船の中身と、スカウトの位置の相互作用**（2026-08-03 に方針だけ決めて**棚上げ**）。
  棚上げの理由（ユーザー判断）: 全カテゴリの VP 換算だけでも相当重く、こちらも
  ユーザーの確認が要るため。**値の入力が終わって評価の物差しが固まってから着手する**
  （いま作っても、掛け先の桁が動いている最中なので検証できない）。
  船に乗る基本技術・金枠同盟は `lfShip` カテゴリで**どの船かに関係なく**評価している。
  ルール上は船へ到達しないと取れないので、「スカウトがどの船の近くにいるか」（Map 側の
  船接触・船星系）と「その船に何が乗っているか」（Setup 側）の組み合わせに意味がある。
  実現すると **Map と Setup をまたぐ最初の評価軸**になる（いまは完全に分離している）。
  - **土台はすでにある**。`evaluateSoft` は船ごとに集計しており（`perScout`）、
    `wScoutByScoutKey` / `wScoutCoreByScoutKey` で船4隻それぞれに別の重みを手で
    設定できる（`page.tsx`）。またぐ評価とは、**この重みをセットアップの中身から
    自動で決める**こと。
  - **段階1（現実的）**: 検索は今のまま。List のセット提案を採点するときだけ、
    `mapValueByFaction` へ「船 s に接触している惑星の評価値 × 船 s に乗っている
    タイルの種族別価値」を足す。古い保存候補は内訳を持たないので0になる
    （色優遇を入れたときと同じ扱い）。
  - **段階2（重い）**: 検索そのものに反映する。ただし船の重みを種族ごとに変えると
    Map スコアが18通りになり、30000シード×18 は現実的でない。やるなら
    「代表種族を選んで検索する」形になる。
  - 難所は**掛け算にするか加重和にするか**。掛け算だと船に近づけない盤面で船タイルの
    価値が0になる（極端）。実際にはどの船へも行けることが多いので、まず船接触度の
    相対値で重み付けする形（正規化）から試すのが安全。

- ~~**LF の「惑星の種類による得点」パワーアクションが 3→2**~~ → **取り下げ**
  （2026-08-03 ユーザー了承）。常設アクションでセットアップのランダム性に関わらない
  ので、このツールに評価軸として入る場所が無い。拡張版の値をレビューするときに
  種類系タイルの値へ織り込む、という**観点**としてだけ残す。

- **勢力ボードの初期研究レベルを読み取る**（2026-08-03 ユーザー指示で TODO 化。
  **今すぐ必須ではない**）。各種族はゲーム開始時に特定の研究トラックが1レベル
  進んだ状態で始まるが、この情報がルールブックの別表に無く、勢力ボードの画像に
  しかないため未確認のまま。
  - **ルールブックの画像ページから読み取るのが早い**（基本版 p20-21・LF p13 の
    勢力ボード図。`pypdfium2` でレンダリングして読む手はすでに使っている）。
  - 効くのは「その列に置かれたタイルをその種族が取りに行けるか」——
    `techPositionWeights.ts` と `advancedTechWeights.ts` の研究列ごとの値。
    いまは能力からの推定で入れているので、実データがあれば裏付けになる。
  - 18種族ぶん揃えば、値のレビューのときに列ごとの差を決める基準になる。

### コード側で根拠が薄い箇所（2026-08-03 の棚卸し）

- **`criterionScore` の 0.2 と 0.3**（`src/gaia/eval/factionEval.ts`）。
  opposeMap / alignMap のタイブレーク係数 0.2 と、topBalance の「上位Kと残りの差を
  少し好む」0.3 は、根拠が書かれていない唯一のマジックナンバー。他の定数
  （`EXTRA_BEST_FACTOR` 2.75 / `DISTANCE_FALLOFF` 1 / `CLUSTER_TRANSDIM_WEIGHT` 0.5 /
  `SETUP_COLOR_PREF_W` 0.3 / `STD_TECH_SCALE` 6 / `ROUND_SCORING_SCALE` 4）は
  実測か理由がコメントに残っている。基準の並びを実際に見て決め直すなら
  `_probe_*` と同じ作りで測れる。
- **`SETUP_WEIGHT_MIN` / `MAX` = -90..90 のコメントが「基準10に合わせて」のまま**
  （`setupWeights.ts`）。基準は 2026-08-01 に 5 へ下がっている。範囲を狭めると
  保存済みの評価指数がクランプされるので、**据え置きでよいかだけ確認したい**。

## 決定済み（対応しない／確定）

- ~~**ラウンド得点の曲線は後半寄り、同盟だけ別曲線**~~ → **2026-08-02 に曲線ごと廃止**
  （タイル×ラウンド×種族の表へ移行）。**以下は値を入れるときの参考**として残す ——
  新しい表の雛形はラウンド差ゼロなので、下の考えかたを種族ごとに書き直す形になる。
  当時の曲線（10分率・合計60）: 序盤 12/11/11/10/9/7 ／ 中盤 6/8/11/13/12/10 ／
  終盤 6/8/9/11/13/13 ／ 終盤(R1困難) 1/5/9/13/16/16 ／ 一定 10×6。
  - **全体を後半寄りに倒す**。以前は「その行動が起きやすいラウンド」だけを見ていたが、
    後半ほど資源も行動数も多いので同じタイルでも後半のほうが数が出る。さらに後半の
    タイルは狙って合わせられるのに対し、R1 には合わせる余地がない。
    序盤 14/12/11/9/8/6 → **12/11/11/10/9/7**、中盤 7/9/12/13/11/8 → **6/8/11/13/12/10**。
  - **同盟（RS08）を終盤型から分けた**。R1 に同盟を作れるのはダー・シュワーム人
    （と、戦略的価値は低いがグリーン人）だけで他種族にはほぼ0価値なので、
    R1 を 1 まで落とした専用曲線 **1/5/9/13/16/16**。
    学院・惑星首府（RS04）は R1 でも現実的に達成できることがよくあるので終盤型のまま
    （6/8/9/11/13/13）。
  - 合計60（平均10）は全曲線で保っているので、カテゴリ全体の影響力は変わらない。
    実測でもラウンド得点の種族間SDは 6.7 → 6.8 でほぼ不変、影響力の順も維持。
  - **割り当ては同日にもう一度直した**（ユーザー指示）: 惑星改造（RS09）は段階を積むのに
    先に鉱山と改造技術が要るので**序盤 → 終盤**へ。ガイア惑星の鉱山（RS05/RS06）と
    未入植系（RS10/RS11）は**どのラウンドでも同じように取れる**ので一定へ。
    曲線が付くのは 鉱山 RS01＝序盤 / 交易所 RS02・RS03 と研究所 RS12＝中盤 /
    学院 RS04 と惑星改造 RS09＝終盤 / 同盟 RS08＝終盤（R1 ほぼ0）の8枚だけになった。
  - **廃止の理由**: 曲線は種族共通なので「同盟の R1 はダー・シュワーム人だけ取れる」
    といった種族差を表せず、`TILE_FACTION_WEIGHTS` 側へまとめて押し込むしかなかった。
    表なら種族ごとに書ける。

- **原始・小惑星の補正値は 2.75 のまま据え置き**（2026-08-02 調査。`EXTRA_BEST_FACTOR`）。
  調査は `scripts/_probe_extra_best_bias.ts`（LF 3p/4p 各60盤面・既定の評価指数）。
  - **種別ごとには分けない**。PROTO と ASTEROID の最良値の差（34.83 vs 43.93）は
    個数の差ではなく**置かれ方の質の差**だった。惑星の個数で層別しても同じ個数どうしで
    ASTEROID が +9〜10 高く（5個 33.65 vs 43.00 / 6個 35.60 vs 45.20 /
    7個 34.00 vs 44.18）、個数が増えても最良値はほとんど上がらない
    （PROTO は2個 34.60 ≒ 7個 34.00）。分けるとこの実在する差を消してしまう。
  - **決め方を変えても値がほぼ動かない**。「基本7色の平均に合わせる」（＝2.743）以外に
    「基本色の *色ごとの合計 ÷ 4軸の最良1惑星* ＝ 2.875 を換算率として使う」という
    決め方でも同じ帯に入る。0.25刻みならどちらも 2.75〜3.00 で、現行値はその間。
  - 副産物: 「最良の1惑星」で見ると基本7色は 37.57 で、PROTO 34.83 / ASTEROID 43.93 と
    もともと同じ土俵に乗っている（-7% / +17%）。
  - **効き先は3つ**（表示専用ではない）: 内訳表の追加行 / Map の色優遇スコア
    （pref≠0 のときだけ）/ List の種族優遇の掛け先（`mapValueByFaction`）。
    したがって PROTO 母星の2種族は Map 評価値が構造的に約1割低く出るが、
    これは盤面の実態の反映なので正規化しない方針（案C）と整合する。

- **Lost Fleet は種族を4つ追加する**（2026-07-30 ユーザー訂正）。
  モウェイド人 / スペースジャイアント（母星＝原始惑星）、
  ティンカーロイド / ダルカニア人（母星＝小惑星）。
  以前コードにあった「LF は種族を追加しない」は誤り。FactionDef.color に
  PROTO / ASTEROID を追加し、マップ側の母星色カウントもこの2種を数える。
- **原始・小惑星の最外周/外周は種族評価に効かない**（2026-07-30 ユーザー確定）。
  内訳表では「-」を出し、追加行の合計にも入れない。
- **Setup の条件指定は「タイルをクリック」に一本化**（2026-07-30）。
  上級技術の回避/強制、船の距離タイル、リベリオンの金枠同盟、経済調整タイルの面、
  拡張部の面 —— すべて専用プルダウンを廃止し、タイル/面をクリックして
  固定・除外・候補・デフォルト（面はランダム/各面）から選ぶ形に統一した。
  キュレーション済みの除外は `defaultAdvancedTileRules()` が既定値として持つ。
- **SETUP_CATALOG の並びは挙動順**（2026-07-30 ユーザー確定）。
  収入 → 即時獲得 → アクション → パス時 → 条件トリガー → 順位 → その他。
  配列順はシャッフルの入力なので、並べ替えで全シードの出目が変わる
  （保存済みセットアップが変わることは了承済み。golden 値を更新した）。
- **拡張部の面の既定はランダム**（2026-07-30）。以前の人数依存 "auto" は廃止。

- **Setup/List の評価表示の形**（2026-07-30、ユーザー選択）。
  種族14行＋行背景＝母星色（7色に集約しない）／評価指数はカテゴリ別係数
  （上級・ブースター・ラウンド得点・最終得点・同盟・標準技術(列)・
  標準技術(自由)・LF船の8つ。当初案の7つに「同盟タイル」を足した）／
  指数は表示だけでなく List のセット提案の選定にも効かせる。
  既定値は導入前と同じ計算になる値なので、既存の保存済み提案とも整合する。

- **マーカーはセル単位で置く**（2026-07-30、実測で確定。前日の「タイル単位で妥協」を撤回）。
  撤回の理由: 「無理」と判断した根拠（24通り総当たりで盤面が成立しない）は
  *グローバルなセル格子の再構成* の話で、これは今も事実。しかしマーカーに必要なのは
  「タイル画像の中のどこにセルがあるか」であって、こちらは測れる。
  実測（`scripts/measure_sector_artwork.py`、全39セクター）:
  アートワークはセル群の外接矩形でぴったり切り出されており、格子の間隔と向きは
  画像クラスごとに一定 —— 650x705(19セル) pitch 81.33 / 向き +30度、
  283x286(3セル) pitch 81.70 / +120度、165x142(1セル) pitch 82.24 / +30度。
  アスペクト誤差 0.02〜0.63%。確定した向きで各セル位置の色をサンプルすると、
  惑星種別ごとに sd=(1,1,1) 程度に収束する（＝惑星の中心に当たっている）。
  実装は `src/gaia/viewer/artworkCalib.ts` ＋ MapBoardViewer の `markerPosArtwork`。
  ブラウザ側でも `getScreenCTM` 経由で PNG 画素を引いて検証済み（650x705 は 35/35 一致）。
  座標を出せないタイルだけ従来のタイル単位強調へフォールバックする（色ごとに重ねて描く）。
  残置物: `MARKER_RADIAL_CALIBRATION` と `markerPosLocal` は模式表示用に生存。
- **評価指数サマリ行のクリック配線**: 実装しない。列ヘッダの◎で代替できることを実機確認。
  実測: 最外周◎→該当5惑星、外周◎→該当12惑星、船接触◎→得点に効いた惑星すべて。
- **模式表示のセクタ番号**: 内部 id をそのまま表示する（`07b` など裏面は b 付き）。変換不要。
- **種族スコアの正規化**: 行わない（案C）。平均値の差（4.06〜11.56）は
  「タイル関与の広さ＝強さ」と見なす立場で確定。
- **新評価軸（ガイア近接・星系クラスタ）の Lost Fleet 反映**: **既定 ON**
  （2026-07-30 ユーザー確定。2026-07-25 は opt-in で導入したが方針変更）。
  `applyExtraAxesLF` を OFF にすると2軸がキーから外れ、旧LFキー（byte不変）の
  結果バケットに戻る。既定 ON なので、以前のLF保存結果とは別バケットになる。
- **表示側「見た目のズレ」(旧Phase B)**: 対応なしで確定。評価側ジオメトリは
  uiqoo collision.js とルールブック双方で正当性を確認済み、現行表示も正常。
- **スカウト配置**: 3人=合法5通り／4人=合法21通り（旧14→21）で確定。
  規則生成テスト（placementFromSeed.scoutLegal.test.ts）でドリフト検出。
- **ラウンド得点「学院/首府+5VP が同一2枚」**: 実物確認済みで確定。
- **衛星駒の順**: モウェイド／ティンカーロイドで共有の1列（ルールブックの
  「同じスペースにもう1個ずつ置く」記述による）。種族別に分けない。

## 参考

- マップ素材 map_tile_01..10 / map_deep_* / map_interspace_* は uiqoo に存在
  （未DL・名前判明済み）— 画像ベースのミニ盤面が欲しくなったら利用可。
- 模式表示の船コード: TW=トワイライト / EC=エクリプス / RB=リベリオン / TF=T.F.マーズ。
- 縦積みへ切り替える閾値は `T.narrowPx = 1180`（`src/components/ui/layout.tsx`）。
  Map の isNarrow と同値。左880が最小600まで縮んでも 600+480=1080 なので1180以上は横並び。
- 盤面表示モード（画像／番号＋向き）は Map の「表示設定」と List の「盤面表示」から
  切替。localStorage キー `gaia_tile_mode` を共有するのでタブ間で揃う。
