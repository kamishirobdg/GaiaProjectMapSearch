# TODO

タスク状態の正本。完了したらこのファイルから消し、経緯はコミットメッセージに残す。

## リリース状況

- 2026-07-30: `release/v1.01` を `main` へ FF push（89コミット、`719fd5e..3c4722f`）。
- **その後のコミットが未push**（マーカーのセル単位化・Setup/List の評価表示と
  条件プロファイル・評価指数の再配分・タイル指定・標準技術の base/LF 分割など）。
  件数は `git rev-list --count origin/main..release/v1.01` で数える
  （ここに数字を書くと毎コミットで古くなるのでやめた）。
  次に push するときは `git push origin release/v1.01:main`
  （origin/main は release/v1.01 の祖先なので FF で通る）。push＝Vercel本番デプロイ。

## デプロイ前に見ておくこと（2026-08-02 に仕分け）

実データが要る確認のうち、**壊れていたら本番で機能が使えないもの**だけを抜き出した。
値・見た目のチューニング（下の「要フィードバック」）はデプロイ後でも直せるので、
ここが通ったら push してよい。済んだ項目はこのリストから消す。

1. **List の「Mapで開く」が正しいテンプレで開く**（`?t=` のテンプレ優先）。
   List でセット提案を出し、行の「Mapで開く」を押す。盤面が出て、**マップ下に
   食い違い警告が出ないこと**。「保存済み条件の自動適用にテンプレを触らせない」のが
   直した点なので、**保存済み条件を持っている状態**で試す。
2. **セットアップ→マップ方向が動く**。List で向きを「セットアップ→マップ」にし、
   起点に保存済みセットアップを選んで生成 → 候補が出ること。
   開発環境には保存セットアップが無く、空状態しか通せていない。
3. **基準4「優位」の並び**。List で基準を「4: 優位（マップ上位の人数+2種族が強い）」に
   して生成。上位に出た組み合わせで、マップ側の上位種族がセットアップ側でも
   強く出ているか（基準1「逆優位」の順張り版になっているか）。
4. **マップ候補が重複しない**。Map で検索を何度か回し、候補一覧に同じ盤面
   （同じテンプレ＋同じ配置）が二重に積まれないこと。
5. **Setup の一括探索が積み上がる**。「一括探索」で1000件ほどを2〜3回。上位20件が
   前回とマージされて順当に積み上がること。中止してもそこまでの結果が残ること。
6. **基本版で LF4種族が出ない**。基本版へ切り替え、評価表・上位K種族・セット提案の
   選定に モウェイド人／スペースジャイアント／ティンカーロイド／ダルカニア人が
   出てこないこと（**保存済み条件を持った状態**で）。

### デプロイ直後にやること

- **消えるのは Setup/List 側だけ**（条件プロファイル・一括探索のランキング・List の
  提案とログ）。**Map のピン留め・候補・条件と、Setup の保存リストは残る**。
  本番を開いて実際にそうなっているか確かめる。
- **評価指数の新しい既定値**は、保存済み条件の自動適用で上書きされて出てこない。
  試すときは「保存済み条件」パネルの**「既定値で新規」**を押す（保存済みは消えない）。

## 要フィードバック（実データでの確認待ち）

**ここは値と見た目のチューニング**。直しても壊れないので、デプロイ後に順次で構わない。

- **標準技術の評価値を拡張ごとに分けた**（2026-08-01、ユーザー指示）。拡張の有無で
  場に出るタイルの母集団が変わり、同じタイルでも相対的な影響力が変わるため。
  - `TECH_POSITION_WEIGHTS_BASE`（通常版・基本14種族）＝**ユーザー作成の CSV を反映済み**。
    `TECH_POSITION_WEIGHTS_LF`（拡張版・18種族）＝**2026-08-01 の値のまま。差し替え待ち**。
  - 参照は `techPositionCell(tileId, pos, lostFleet)` を通す。
  - 上級技術など他のタイルは当面共通。ただし**拡張ごとに上書きする仕組みだけ用意した**
    （`TILE_FACTION_WEIGHTS_LF` に書けばそのタイルだけ差し替わる。いまは空）。
  - CSV の反映と検算は `python scripts/gen_tech_position_table.py <csv> [--check]`。
    通常版は756セル全一致を確認済み（両方向）。
  - 通常版の実測（`_probe_faction_totals.ts --base`）: 最大イタル人114.3 /
    最小バルタック人74.2 = **1.54倍**（拡張版の5.3倍に比べてかなり均等）。
    影響力の順も 技術 > 上級 > ブースター > ラウンド > 最終 > 同盟 で狙いどおり。
    盤面ごとの最高値は中央値132 / 最大160、小数は0件。
  - **確認してほしいこと**: 通常版と拡張版で桁が揃っているか（通常版の中央値132に対し
    拡張版は128）。揃えたいなら `STD_TECH_SCALE` を拡張ごとに分ける手もある。

- **評価表の列ラベルを2文字に短縮した**（2026-08-01）。ブースター→**RB** /
  ラウンド→**R点**。テーブルは固定幅ではなく内容に合わせて伸びるので、
  長いラベルの列だけ広がって数値の並びが不揃いになっていた。
  LF船は「LF」がラテン文字で漢字のおよそ半分の幅なので、全体では漢字2文字ぶん＝
  技術・上級・最終などと同じ幅に収まる想定で据え置いた。**見た目の確認待ち**。

- **Setup の評価値を18種族×全タイル見直した**（2026-08-01。「次にやること2」を実施）。
  根拠はルールブックの実文にそろえた: 基本14種族＝基本版 p20-21「別表Ⅰ：勢力」、
  LF4種族＝LF p13「別表Ⅰ：新たな勢力」、原始惑星／小惑星の入植コスト＝LF p10。
  **影響力は狙いの順になった**（4人LF 300件の種族間SD。`scripts/_probe_category_influence.ts`）:
  技術22.2 > LF船19.7 > 上級17.8 > ブースター14.9 > ラウンド13.5 > 最終9.7 >
  追加上級6.9 > 同盟3.6（係数調整前は 技術が6位・ラウンドが2位だった）。

  **仕組みの整理**: タイルの重みは「そのタイルとその種族の噛み合い」だけを表し、
  「そのカテゴリの1枚がゲーム全体にどれだけ効くか」は**評価指数の既定値**で表す、と
  役割を分けた。既定値が基準から外れるのは2つだけ ——
  standardTech 6（基準の1.2倍。9枚が必ず出て最後まで残る）、
  roundScoring 4（基準の0.8倍。効くのは6ラウンドのうち1つだけ）。

  **ルールブックで判明して直した主なもの**（値を見るときの手がかり）:
  - ランティダ人の「他プレイヤーの惑星に置く鉱山」は**惑星の種類にもガイア惑星の数にも
    数えない**。種類系のタイル（AT15/FS03/RB12/RS11/TS2/ART07）から外した。
    **除外はこの「数を数える」系だけ**（基本版 p20「別表Ⅰ：勢力」に
    「他のすべての点においては通常の鉱山と見なされます」と明記されている）。
    **2026-08-02 訂正**: TS7（ガイア惑星に鉱山を建設するたびに＋3VP、p8）は
    「数える」系ではなく「建設したときのトリガー」系なので除外対象外 —
    ランティダ人が他プレイヤーの入植済みガイア惑星に鉱山を置いても発動する。
    以前はこれもTS2などと同じ扱いで除外してしまい、
    `TECH_POSITION_WEIGHTS_BASE.TS7` の lantids が全列0のままになっている
    （CSV反映時に引き継がれた誤り）。AT14（同じ「鉱山を建設するたび＋3VP」効果、
    lantids:2）と平仄を合わせて、次のユーザーの値レビューで直す。
  - バル・タック人は**航法を進められない**。深宇宙・宙域系（AT17/AT21/FS05/FS09/
    RB13/RS10/ART06）を全部マイナスにした。以前は1つも入っていなかった。
  - スペースジャイアントの「無料2段階改造＋鉱山建設」は**ラウンドごとに1回**（p10）。
    通常惑星が常に2段階なので毎ラウンド無料で鉱山が増える＝鉱山数・惑星種類・
    改造段階の主役に引き上げた。惑星首府の技術タイルは逆に**1回だけ**。
  - ティンカーロイドの RB09/AT16 は「**建設済みの**首府・学院1つごと」の数え上げなので、
    初期配置の首府が最初から乗る（＋）。RS04 だけが「建設したとき」の誘発で取り逃す（−）。
    以前は RB09 が −1 で AT16 が +2 と矛盾していた。
  - グリーン人は**QICを得られない**。QIC付きのタイル（TS1/RB02/FED8Q/FEDG5/ART09）を
    マイナスに、AT20（QICアクションのたび+4VP）は −2 にした。
  - フィラク族の「研究所→交易所の格下げ」は**「交易所への改良」と見なされる**ので、
    交易所を建てるたび系（AT11/RS02/RS03/RB08/AT09）が毎ラウンド誘発する。
  - ダルカニア人（通常惑星が全色1段階）と RB04（無料1段階）はコストがちょうど一致。
    ジオデン人のPIと RS11、ダルカニア人のPIと RS10 も誘発条件が完全一致。

  **確認してほしいこと**:
  1. **ダルカニア人が強く出すぎていないか**。母星色ごとの平均は
     小惑星110 / 白89 / 原始84 / 橙75 / 黄72 / 茶72 / 青71 / 黒65 / **赤48** で、
     赤（ハッシュ・ホラ／ダー・シュワーム）との差が2倍以上ある。正規化しない方針（案C）
     なので差が出ること自体は設計どおりだが、この幅が感覚と合うか。
  2. **standardTech 6 / roundScoring 4 という既定値の差**が納得できるか。
     一律5に戻すと 技術が3位・ラウンドが4位になる。
  3. 個々の値。編集用テーブルは `src/gaia/eval/factionWeights.ts` の
     `TILE_FACTION_WEIGHTS`（86タイル）と `TECH_POSITION_WEIGHTS`（9タイル×7配置）。

  **付随して変えたもの**:
  - 評価値の桁: 見直しで非ゼロのセルが増えたぶん膨らんだので、**基準の係数を 10 → 5**
    に下げて元の狙い（100前後）に戻した。実測で盤面ごとの最高値が中央値112・最大179
    （`scripts/_probe_score_range.ts`）。小数は0件。
  - ラウンド得点は**タイル1枚ごとに整数へ丸める**ようにした。係数が10の倍数でないと
    「重み×倍率×係数/10」が小数になるため。丸めを1枚ごとにするのは、内訳表の合計と
    タイル強調の合計を必ず一致させるため（両方が同じ関数を通る）。
    副作用として、評価指数を2倍にしてもラウンド得点だけはぴったり2倍にならない
    （1枚あたり最大0.5、6枚で±3のズレ）。
  - Setup の色優遇の係数を **0.25 → 0.3**（`measure_setup_color_pref.ts` を再実行。
    掛け先の振れ幅が 53.1 → 37.7 に縮んだため）。目盛りの文言は従来どおり。
  - **保存データ**: IndexedDB v10 / localStorage マーカー v4。**今回は Map 側の重みを
    触っていないので、消えるのは Setup/List 側だけ**（条件プロファイル・一括探索の
    ランキング・List の提案とログ）。**Map のピン留め・候補・条件は残る**。
    Setup の保存リストも残る（スコアは表示のたびに計算し直すので新しい値で出る）。

- **評価表の列の並びとラベル**（2026-07-31）。並びは影響力の大きい順
  （技術 / LF船 / 上級 / ブースター / ラウンド / 最終 / 拡張 / 同盟）にした。
  横スクロールで最初に隠れるのが影響の小さい列になる、という以前からの意図は同じ。
  ラベルは「標準技術→技術」「追加上級→拡張」。**列幅は1文字あたり約16px**で、
  LF・4人だと表は744px／枠745pxと余白がほぼ無い。残る長いラベルは
  ブースター(102px)とラウンド(86px)で、2文字にすれば合計約100px空く。
  ただしどちらも影響力が中位でよく見る列なので、短縮するか（例:「Rブスト」
  「R得点」）はまだ決めていない。

- **Setup の評価をタイル配置ベースへ作り直した**（2026-07-31）。
  - **標準技術**: 「トラック下6枚」と「フリー枠3枚」の2テーブル・2係数をやめ、
    `TECH_POSITION_WEIGHTS[タイル][配置][種族]` の1テーブル・1係数（standardTech）に
    統合。配置は研究列6つ＋"free" の7通り。評価指数の列も2つ→1つになった。
    **フリー枠の効き具合が上がっている**（専用の低い係数をやめたため。トラック比
    0.2 → 0.4 相当）。値の見直しは「1つずつ精査」で。
  - **ラウンド得点に並び順を反映**。`ROUND_SCORING_TIMING` が何ラウンド目に出たかで
    倍率を変える（10分率。どの曲線も6ラウンドの合計が60＝平均10なので、全体の
    スケールは従来と同じ）。**曲線は 2026-08-02 に精査して後半寄りへ倒した**
    （下の「決定済み」参照）。
  **確認してほしいこと**: 新しい曲線でセット提案の並びが感覚と合うか。
  とくに同盟タイル（RS08）が R1 に来た盤面で、ダー・シュワーム人以外の評価が
  下がりすぎていないか。

- **色優遇/冷遇を3タブへ広げた**（2026-07-31）。目盛りはどれも
  「0=効かない / 1=基準とほぼ互角 / 2=優遇が主導 / 5=実質それだけで決まる」で揃えてあり、
  文言は実測に基づく（`scripts/measure_color_pref.ts` /
  `scripts/measure_setup_color_pref.ts`。重みを変えたら再実行して文言を合わせ直すこと）。
  - **Map**: 原始・小惑星も対象に。掛け先は extraBest（最良の1惑星×補正値）。
    **wColorPref の既定を 3 → 25 に変えた**。3 のままでは pref=2 が偏り項の0.2倍しか
    効かず、実用域が10〜20だったため。既定の pref は0なので既定の検索結果は不変。
  - **Setup**: 母星色ごと。掛け先は「その色でいちばん強い種族のスコア」。
    係数 0.25（内部固定、入力欄には出さない）。一括探索の並びにだけ効く。
  - **List**: 種族ごと。掛け先は「Map の評価値（母星色ぶん）＋ Setup の評価値」の合計。
    係数は Setup の半分（掛け先が約2倍の大きさになるため）。
  **実データでの確認が未**: 目盛りの体感が文言と合っているか（特に pref=1 と 2 の差）。
  List は Map 候補の評価内訳から Map ぶんを引くので、内訳を持たない古い候補では
  Map ぶんが0になる（Setup ぶんだけで効く）。

- **評価値の桁をゲームの得点に合わせた**（2026-07-31）。色ごと／種族ごとの評価値が
  100前後、Map と Setup を足して200前後になる（実測: Map 最大101 / Setup 最大123）。
  小数はすべて消してある。**貯めてあった結果・条件は全部消えている**
  （IndexedDB v9 の upgrade と localStorage の一度きりの掃除。ユーザー確定済み）。
  要注意点が3つ:
  - **距離減衰 `DISTANCE_FALLOFF` は重みのスケールと連動する。** 船接触・船星系は
    「重み −（距離−1）×これ」で引く量が絶対値なので、重みだけ動かすと減衰の形が
    変わって評価の意味が変わる。**スケールを変えるときは必ず一緒に変えること。**
  - **星系の軸は色ごとの合計で丸めている。** 次元横断を0.5で数えるため小数が出る。
    クラスタ単位ではなく合計で丸めるので 0.5 が2つあれば打ち消し合うが、
    0.5 が1つだけ残る盤面では切り上がる（回帰スナップショットの base_34p 90件が
    最大8.94%動いた。LF 60件は不変）。
  - **Setup のフリー枠係数の比率が動いた**（基準の 1/4 → 1/5）。基準10では 2.5 が
    整数にならないため。「トラック分より軽い」意図は保っている。1/4 を厳密に保つには
    基準20（評価値が最大280前後）が必要で、100前後という狙いから外れる。
  平均・散らばり・一括探索の基準値は割り算なのでもともと小数。整数へ丸めている
  （この丸めの粒度が実用的かは要確認）。

- **原始・小惑星の「最良の1つ×補正値」**（2026-07-31 実装）。内訳表の追加行が
  種別ごとの単純合算から「4軸（船接触＋船星系＋ガイア＋星系）の合計がいちばん大きい
  1惑星 × 補正値」に変わった。行クリックのマーカーもその1惑星だけを光らせる。
  **補正値 2.75 は 2026-08-02 に調査して据え置きと結論**（下の「決定済み」参照）。
  検索スコア本体には入らないが、**色優遇を付けたときの Map スコアと、List の
  種族優遇の掛け先には効く**（当初「表示専用」と書いていたのは不正確だった）。
  既定は pref=0 なので、既定の検索結果・保存済みの結果・回帰スナップショットは不変。

- **Setup の一括探索＋ランキング**（2026-07-31 実装）。いまの条件（タイル指定・面の
  指定込み）で大量生成し、基準の良い順に上位20件を **IndexedDB に保存**する。
  次の探索は前回の上位とマージしてまた上位20件を残す（Map の candidates と同型）。
  基準はマップ非依存の2つ（上位バランス／全体バランス）をプルダウンで選び、
  結果は条件×基準ごとに分かれる。件数と基準は localStorage に保存。
  生成は 0.25ms/件 と軽いのでワーカーは使わず、250件ごとに setTimeout で UI を
  明け渡すチャンク実行にした（最大5000件、進捗と中止あり。中止してもそこまでの
  上位は保存される）。行クリックで表示、行の「記録」で保存リストへ入る。
  表示位置は種族別評価の下（ユーザー指定）。
  **実データでの確認が未**: 使う件数の感覚（1000で足りるか）と、上位20件という
  数が実用的かどうか。何回か探索して上位が順当に積み上がるか。

- **基本版で LF4種族を出さない**（2026-07-31）。評価表・上位K種族・セット提案の
  選定・散らばりの母集団すべてから外し、惑星改造ボードの衛星駒の順も基本版では
  隠した。合成データでは確認済みだが、保存済み条件がある実データでの見え方が未。

- **LF4種族の行背景をマップのマーカー色に合わせた**（2026-07-31）。
  原始惑星 #3d8cb5 / 小惑星 #9c4a8f。基本7色の行背景より濃いので文字は白にしてある。
  見え方（濃さ・白文字の可読性）は目視での判断が要る。

- **LF4種族の重み DRAFT**（2026-07-31 投入、`factionWeights.ts`）。**値のレビュー待ち**。
  根拠は LF ルールブック（`web_GP_LostFleet_JPN.pdf`「別表Ⅰ：新たな勢力」13ページ）の
  能力のみ。アーキタイプは モウェイド人=パワー経済・同盟形成 /
  スペースジャイアント=惑星改造・惑星種類 / ティンカーロイド=初期首府の追加アクション /
  ダルカニア人=広域展開。
  **いちばん弱いのは研究列の親和度**（TECH_TRACK_WEIGHTS）。勢力ボードの初期資源・
  初期研究レベルが PDF の画像ページで読めておらず、能力からの推定で仮置きしてある。
  実測（LF4人24盤面の平均・18種族中）: ダルカニア人1位12.11 / モウェイド人5位9.60 /
  スペースジャイアント7位9.01 / ティンカーロイド13位7.49。
  既往の帯（4.06〜11.56）にほぼ収まる範囲。正規化しない方針（案C）は維持。

- **`?t=` のテンプレ優先と食い違い警告**（2026-07-31 修正）。`/board?h=...&t=` で
  開いたときは保存済み条件の自動適用にテンプレを触らせないようにし、盤面と
  テンプレのスロット集合が食い違ったらマップ下に警告を出すようにした。
  合成プロファイルでの A/B は済んでいるが、実データ（ユーザーの保存済み条件）で
  List の「Mapで開く」が正しいテンプレで開くかの確認が未。

- **評価指数の既定値を変更した**（2026-07-30）。船接触 6→10、ガイア距離2 3→8、
  距離3 1→3（他は据え置き）。各軸が全体に占める割合を24盤面で実測して、
  影響力が 船接触 > 船星系 > ガイア > 星系 > 最外周 > 外周 の順になるよう決めた。
  保存済みの検索条件プロファイルは起動時に自動適用されるため、そのままでは
  新しい既定値が出てこない。**「保存済み条件」パネルの「既定値で新規」**を押すと
  条件が既定へ戻り、自動適用のポインタも消える（保存済みのマップ・条件は消えない）。

- **セットアップ→マップ方向／保存済みから選ぶ**: 開発環境にピン留め・保存セットアップが
  無く、空状態と合成データでしか通せていない。実データでの動作確認が未。
- **マップ候補の重複除去**: 盤面（テンプレ＋placementHash）単位に修正済み。
  再現ケースは `src/lib/mapCandidates.test.ts` で固定したが、実データでの目視確認が未。
- **基準4「優位」(alignMap)**: マップ上位 K=人数+2 種族がセットアップでも強い組み合わせを
  選ぶ（基準1「逆優位」の順張り版）。セットアップ→マップ方向で候補がマップ側の優劣で
  並ぶようになったかの確認が未。
- **マーカーのセル単位表示**: 画像表示でのリングの見え方（大きさ・線の太さ・点滅の強さ）。
  位置が正しいことは実測とブラウザ検証で確認済みだが、視認性は目視での判断が要る。
  リング色は PROTO(#3d8cb5) / ASTEROID(#9c4a8f) / GAIA(#3fae2a) / TRANSDIM(#b25ce0)
  をタイル画像から採色。GAIA/TRANSDIM は星系マーカー用に追加したもので未確認。
- **ハローの強さ**: 同色の惑星に埋もれないよう、リングの下に明度が反対の縁取りを
  敷いた（暗いリング→白、明るいリング→黒。太さはリングの2倍）。太さ・不透明度は
  目視で要調整。他の案は引き継ぎ時の会話を参照。
- **Setup/List の評価表示**（2026-07-30 実装）: 種族14行の表・総合スコアのサマリ・
  評価指数の入力を左ペインへ追加。開発環境では合成データでしか見ていないので、
  実データでの見え方（列幅・14行の高さ・保存リストとの並び）と、評価指数を
  いじったときのセット提案の変化が期待どおりかの確認が未。

## 残タスク

### 次にやること（2026-07-31 ユーザー指示。この順で）

1. ~~**原始惑星・小惑星の補正値が妥当かの調査**~~ → **2026-08-02 実施済み**。
   結論は **2.75 のまま据え置き**（下の「決定済み」参照）。

2. ~~**Setup の評価値を1つずつ精査する**~~ → **2026-08-01 実施済み**。
   上の「要フィードバック」を参照。影響力は狙いの順になった。残っているのは
   ユーザーによる値のレビューだけ。**まだ手を付けていない下位項目**:
   - ~~`ROUND_SCORING_TIMING` の曲線と割り当て~~ → **2026-08-02 に精査済み**
     （下の「決定済み」参照）。
   - 勢力ボードの**初期研究レベルが今も未確認**（ルールブックの別表になく、
     勢力ボードの画像にしかない）。研究列の親和度はそのぶん能力からの推定を含む。
     実物かボード画像が手に入れば `TECH_POSITION_WEIGHTS` の精度が上がる。

### 直近の依頼で未着手

- **ラウンド得点を「タイル×ラウンド×種族」のCSVにする**（2026-08-02 ユーザー確定・実装待ち）。
  研究タイル（`TECH_POSITION_WEIGHTS`）と同じく、どのタイルが何ラウンド目に置かれたかで
  評価値を個別に持たせる。
  - **曲線（`ROUND_SCORING_TIMING`）は廃止**。CSV がラウンド別の値を直接持つので倍率の
    掛け算が要らなくなる。`roundTimingOf` / `ROUND_TIMING_BASE` と、
    `roundScoringContrib` の「1枚ごとに丸める」処理もここで見直す。
  - **通常版と拡張版で分ける**（標準技術と同じ）。通常版 9タイル×6ラウンド×14種族＝756セル、
    拡張版 12×6×18＝**1296セル**。
  - 雛形は「**今の共通評価値（`TILE_FACTION_WEIGHTS` の RS 行）を全ラウンドに複製**」した
    状態で生成し、変えたいセルだけユーザーが直す。
  - 生成・検算スクリプトは `scripts/gen_tech_position_table.py` と同じ作りにする
    （`--check` で全セル突き合わせ）。CSV の形は
    `対応表 / タイル / ラウンド / 種族名×18` でサンプルを Discord へ送付済み。
  - 2026-08-02 に直した曲線の割当（ガイア鉱山 RS05/06・未入植 RS10/11＝一定、
    惑星改造 RS09＝終盤）は、**CSV に置き換わるまでの暫定**。

- ~~**Setup の研究トラックを6列に収める**（案A）~~ → **2026-08-02 実装済み**。
  研究トラックの見出し横のボタンで「横スクロール（既定）／画面幅に収める」を切り替え、
  選択は localStorage `gaia_setup_layout` に持つ。幅は CSS だけで決めていて
  （列を `flex:1 1 0 / minWidth:0 / maxWidth:TILE_COL_W`、タイルは CSS 変数
  `--setup-tile-w` `--setup-tile-max` で列幅に追従）、JS で画面幅を測らないので
  SSR とのちらつきが出ない。**Android 実機での確認待ち** —— 6列が読める大きさに
  収まるか、上部スロットの確保高さ（`"fit"` では 148→96 の暫定値）が余りすぎないか、
  PC 幅では見た目が変わらないこと。

### 判断待ち（着手前にユーザーへ）

- **旧ルール入力の後片付け**（低優先）。UI を全スロットのタイル指定へ統合したので、
  `BuildSetupInput` の `avoidRules` / `forceRules` / `forceTileRules` /
  `allowTileRules` / `shipDistanceAvoid` / `shipDistanceForce` / `rebellionGoldFed`
  は**もう誰も設定しない**（エンジンと共有リンクの復元だけが読む）。
  `AVOID_RULES` は既定の除外を作るためだけに残っている（`allowTiles` は未使用）。
  - **条件サマリは 2026-08-02 に対応済み**。旧フィールドは0件で行ごと出ないため
    タイル指定を入れても条件を見分けられなかったので、`countTileRules`
    （`src/gaia/setup/tileRules.ts`）で「固定N / 候補N / 除外N」を出すようにした。
    除外だけは既定との差を数える（既定の除外はどの条件にも同じだけ入っているため）。
  - **残っているのは型からフィールドを消すかどうか**。消すと古い共有リンクの復元が
    落ちるので、移行の要否とセットで判断する。
- **List のミニ盤面にはマーカーが出ない**: 評価表クリックのタイル点滅は Setup 専用。
  List の提案プレビュー（SetupBoard/SmallTile）は対象外のままにしてある。

- **スカウトの船別色**（要判断）: 現状マーカーは惑星色で統一。船ごとに色を割り当てるか、
  どの船由来かはポップアップだけで示すか。
- **種族別重みの値レビュー**（`src/gaia/eval/factionWeights.ts`）
  正規化は行わない方針（案C）で確定。値は編集用テーブル（TILE_FACTION_WEIGHTS /
  TECH_TRACK_WEIGHTS）を直接調整する。カテゴリ単位の効き具合は画面の
  「評価指数」から調整できる（既定は STD_TECH_TRACK_SCALE(0.5) /
  STD_TECH_FREE_SCALE(0.25)、他は 1.0）。

## 検討事項（着手前にユーザー確認）

- ~~ラウンド得点のタイミングを種族ごとに設定する~~ → **CSV 方式で確定**。
  上の「直近の依頼で未着手」に一本化した（曲線に種族の例外を足すのではなく、
  タイル×ラウンド×種族の表を作って曲線ごと置き換える）。

- 3人時の 09/10 除外縮小盤 `base_3p_small`（要望があれば別テンプレートで）。
- 種族別総合評価タブ（マップ＋setup合算の一覧表示。セット提案の重み確定後）。

## 決定済み（対応しない／確定）

- **ラウンド得点の曲線は後半寄り、同盟だけ別曲線**（2026-08-02 ユーザー確定。
  `ROUND_SCORING_TIMING`）。
  - **全体を後半寄りに倒す**。以前は「その行動が起きやすいラウンド」だけを見ていたが、
    後半ほど資源も行動数も多いので同じタイルでも後半のほうが数が出る。さらに後半の
    タイルは狙って合わせられるのに対し、R1 には合わせる余地がない。
    序盤 14/12/11/9/8/6 → **12/11/11/10/9/7**、中盤 7/9/12/13/11/8 → **6/8/11/13/12/10**。
  - **同盟（RS08）を終盤型から分けた**。R1 に同盟を作れるのはダー・シュワーム人
    （と、戦略的価値は低いがグリーン人）だけで他種族にはほぼ0価値なので、
    R1 を 1 まで落とした専用曲線 **1/5/9/13/16/16**。
    学院・惑星首府（RS04）は R1 でも現実的に達成できることがよくあるので終盤型のまま
    （6/8/9/11/13/13）。
  - 合計60（平均10）は全曲線で保っているので、カテゴリ全体の影響力は変わらない。
    実測でもラウンド得点の種族間SDは 6.7 → 6.8 でほぼ不変、影響力の順も維持。
  - **割り当ては同日にもう一度直した**（ユーザー指示）: 惑星改造（RS09）は段階を積むのに
    先に鉱山と改造技術が要るので**序盤 → 終盤**へ。ガイア惑星の鉱山（RS05/RS06）と
    未入植系（RS10/RS11）は**どのラウンドでも同じように取れる**ので一定へ。
    曲線が付くのは 鉱山 RS01＝序盤 / 交易所 RS02・RS03 と研究所 RS12＝中盤 /
    学院 RS04 と惑星改造 RS09＝終盤 / 同盟 RS08＝終盤（R1 ほぼ0）の8枚だけになった。
  - **この形（タイル単位の曲線）は暫定**。上の「直近の依頼で未着手」にあるとおり、
    タイル×ラウンド×種族の CSV に置き換えて曲線ごと廃止する。いまは「同盟の R1 は
    ダー・シュワーム人だけ取れる」といった種族差を曲線では表せず、
    `TILE_FACTION_WEIGHTS` 側にまとめて押し込むしかない。

- **原始・小惑星の補正値は 2.75 のまま据え置き**（2026-08-02 調査。`EXTRA_BEST_FACTOR`）。
  調査は `scripts/_probe_extra_best_bias.ts`（LF 3p/4p 各60盤面・既定の評価指数）。
  - **種別ごとには分けない**。PROTO と ASTEROID の最良値の差（34.83 vs 43.93）は
    個数の差ではなく**置かれ方の質の差**だった。惑星の個数で層別しても同じ個数どうしで
    ASTEROID が +9〜10 高く（5個 33.65 vs 43.00 / 6個 35.60 vs 45.20 /
    7個 34.00 vs 44.18）、個数が増えても最良値はほとんど上がらない
    （PROTO は2個 34.60 ≒ 7個 34.00）。分けるとこの実在する差を消してしまう。
  - **決め方を変えても値がほぼ動かない**。「基本7色の平均に合わせる」（＝2.743）以外に
    「基本色の *色ごとの合計 ÷ 4軸の最良1惑星* ＝ 2.875 を換算率として使う」という
    決め方でも同じ帯に入る。0.25刻みならどちらも 2.75〜3.00 で、現行値はその間。
  - 副産物: 「最良の1惑星」で見ると基本7色は 37.57 で、PROTO 34.83 / ASTEROID 43.93 と
    もともと同じ土俵に乗っている（-7% / +17%）。
  - **効き先は3つ**（表示専用ではない）: 内訳表の追加行 / Map の色優遇スコア
    （pref≠0 のときだけ）/ List の種族優遇の掛け先（`mapValueByFaction`）。
    したがって PROTO 母星の2種族は Map 評価値が構造的に約1割低く出るが、
    これは盤面の実態の反映なので正規化しない方針（案C）と整合する。

- **Lost Fleet は種族を4つ追加する**（2026-07-30 ユーザー訂正）。
  モウェイド人 / スペースジャイアント（母星＝原始惑星）、
  ティンカーロイド / ダルカニア人（母星＝小惑星）。
  以前コードにあった「LF は種族を追加しない」は誤り。FactionDef.color に
  PROTO / ASTEROID を追加し、マップ側の母星色カウントもこの2種を数える。
- **原始・小惑星の最外周/外周は種族評価に効かない**（2026-07-30 ユーザー確定）。
  内訳表では「-」を出し、追加行の合計にも入れない。
- **Setup の条件指定は「タイルをクリック」に一本化**（2026-07-30）。
  上級技術の回避/強制、船の距離タイル、リベリオンの金枠同盟、経済調整タイルの面、
  拡張部の面 —— すべて専用プルダウンを廃止し、タイル/面をクリックして
  固定・除外・候補・デフォルト（面はランダム/各面）から選ぶ形に統一した。
  キュレーション済みの除外は `defaultAdvancedTileRules()` が既定値として持つ。
- **SETUP_CATALOG の並びは挙動順**（2026-07-30 ユーザー確定）。
  収入 → 即時獲得 → アクション → パス時 → 条件トリガー → 順位 → その他。
  配列順はシャッフルの入力なので、並べ替えで全シードの出目が変わる
  （保存済みセットアップが変わることは了承済み。golden 値を更新した）。
- **拡張部の面の既定はランダム**（2026-07-30）。以前の人数依存 "auto" は廃止。

- **Setup/List の評価表示の形**（2026-07-30、ユーザー選択）。
  種族14行＋行背景＝母星色（7色に集約しない）／評価指数はカテゴリ別係数
  （上級・ブースター・ラウンド得点・最終得点・同盟・標準技術(列)・
  標準技術(自由)・LF船の8つ。当初案の7つに「同盟タイル」を足した）／
  指数は表示だけでなく List のセット提案の選定にも効かせる。
  既定値は導入前と同じ計算になる値なので、既存の保存済み提案とも整合する。

- **マーカーはセル単位で置く**（2026-07-30、実測で確定。前日の「タイル単位で妥協」を撤回）。
  撤回の理由: 「無理」と判断した根拠（24通り総当たりで盤面が成立しない）は
  *グローバルなセル格子の再構成* の話で、これは今も事実。しかしマーカーに必要なのは
  「タイル画像の中のどこにセルがあるか」であって、こちらは測れる。
  実測（`scripts/measure_sector_artwork.py`、全39セクター）:
  アートワークはセル群の外接矩形でぴったり切り出されており、格子の間隔と向きは
  画像クラスごとに一定 —— 650x705(19セル) pitch 81.33 / 向き +30度、
  283x286(3セル) pitch 81.70 / +120度、165x142(1セル) pitch 82.24 / +30度。
  アスペクト誤差 0.02〜0.63%。確定した向きで各セル位置の色をサンプルすると、
  惑星種別ごとに sd=(1,1,1) 程度に収束する（＝惑星の中心に当たっている）。
  実装は `src/gaia/viewer/artworkCalib.ts` ＋ MapBoardViewer の `markerPosArtwork`。
  ブラウザ側でも `getScreenCTM` 経由で PNG 画素を引いて検証済み（650x705 は 35/35 一致）。
  座標を出せないタイルだけ従来のタイル単位強調へフォールバックする（色ごとに重ねて描く）。
  残置物: `MARKER_RADIAL_CALIBRATION` と `markerPosLocal` は模式表示用に生存。
- **評価指数サマリ行のクリック配線**: 実装しない。列ヘッダの◎で代替できることを実機確認。
  実測: 最外周◎→該当5惑星、外周◎→該当12惑星、船接触◎→得点に効いた惑星すべて。
- **模式表示のセクタ番号**: 内部 id をそのまま表示する（`07b` など裏面は b 付き）。変換不要。
- **種族スコアの正規化**: 行わない（案C）。平均値の差（4.06〜11.56）は
  「タイル関与の広さ＝強さ」と見なす立場で確定。
- **新評価軸（ガイア近接・星系クラスタ）の Lost Fleet 反映**: **既定 ON**
  （2026-07-30 ユーザー確定。2026-07-25 は opt-in で導入したが方針変更）。
  `applyExtraAxesLF` を OFF にすると2軸がキーから外れ、旧LFキー（byte不変）の
  結果バケットに戻る。既定 ON なので、以前のLF保存結果とは別バケットになる。
- **表示側「見た目のズレ」(旧Phase B)**: 対応なしで確定。評価側ジオメトリは
  uiqoo collision.js とルールブック双方で正当性を確認済み、現行表示も正常。
- **スカウト配置**: 3人=合法5通り／4人=合法21通り（旧14→21）で確定。
  規則生成テスト（placementFromSeed.scoutLegal.test.ts）でドリフト検出。
- **ラウンド得点「学院/首府+5VP が同一2枚」**: 実物確認済みで確定。
- **衛星駒の順**: モウェイド／ティンカーロイドで共有の1列（ルールブックの
  「同じスペースにもう1個ずつ置く」記述による）。種族別に分けない。

## 参考

- マップ素材 map_tile_01..10 / map_deep_* / map_interspace_* は uiqoo に存在
  （未DL・名前判明済み）— 画像ベースのミニ盤面が欲しくなったら利用可。
- 模式表示の船コード: TW=トワイライト / EC=エクリプス / RB=リベリオン / TF=T.F.マーズ。
- 縦積みへ切り替える閾値は `T.narrowPx = 1180`（`src/components/ui/layout.tsx`）。
  Map の isNarrow と同値。左880が最小600まで縮んでも 600+480=1080 なので1180以上は横並び。
- 盤面表示モード（画像／番号＋向き）は Map の「表示設定」と List の「盤面表示」から
  切替。localStorage キー `gaia_tile_mode` を共有するのでタブ間で揃う。
