# TODO

タスク状態の正本。完了したらこのファイルから消し、経緯はコミットメッセージに残す。
（要望9件は 2026-07-24 受領。①②④⑥⑦⑨は完了済み）

## 未着手

（なし。マップ素材 map_tile_01..10 / map_deep_* / map_interspace_* は uiqoo に存在、
未DL・名前判明済み — 画像ベースのミニ盤面が欲しくなったら利用可）

## 随時反映

- **⑤ マップUI文言改善**（承認済み指摘のうち残り）
  内部名露出／outer・touch の区別説明／保存条件の生テキスト表示／H5の「0=無効」誤読対策／
  用語集→?ツールチップ化／色優遇の説明不足／HASH2種の説明。
  （RankScore=1000+raw の件は生スコア表示化で解消済み）

## レビュー待ち

- **種族別重み DRAFT のレビュー**（`src/gaia/eval/factionWeights.ts`）
  一覧タブ「セット提案」で使用中。タイル×種族14の重み（-2..+2）・マップ親和度
  （ガイア/次元横断）・推奨基準式（factionEval.ts）とマップ式（mapFaction.ts）が対象。
  数値・式の差し替えだけで挙動が変わる（テストは機構のみ固定）。

## 検討事項（着手前にユーザー確認）

- 新評価軸（ガイア近接・星系クラスタ）を Lost Fleet にも反映するか
  （docs/map-base-spec.md §4.5。反映するとLFキーが変わり既存保存結果と別バケットになる）。
- 3人時の 09/10 除外縮小盤 `base_3p_small`（要望があれば別テンプレートで）。
- 種族別総合評価タブ（マップ＋setup合算の一覧表示。セット提案の重み確定後）。

## 実物確認待ち（任意）

- ラウンド得点「学院/首府+5VP が同一2枚」（写真＋uiqooデータで確定済み、実物ダメ押しのみ）。
