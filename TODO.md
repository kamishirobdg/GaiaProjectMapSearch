# TODO

タスク状態の正本。完了したらこのファイルから消し、経緯はコミットメッセージに残す。

## 2026-07-24 大型バッチ（仕様確定済み・順次実装中）

- [x] **A** #5/#6 タブ遷移ちらつき解消 ＋ #4 List マップ/基準の記憶（layout effect復元）
- [x] **B** #2 ランキングに「表示中」バッジ（Map追加／Setupは既存）
- [x] **C** #1 連結許容数の原子/小惑星 → 既に含有と確認（no-op）
- [x] **D** List拡張: #7「Mapで開く」＋提案のミニ盤面/縮小セットアップ画像（SetupBoard新設）／
      #8 提案の保存（localStorage自己完結スナップショット）＋「保存した提案」から即再表示・削除
- [x] **E** #3 Setupを2カラム化（左上=検索条件／その下=ランク、右=セットアップ表示）
      row-reverseラッパー。ルール選択プルダウン等は各タイルに付随のため右カラム内に残置。
- [x] **F** #10 モウェイド/ティンカーロイド 衛星駒ランダム
  - 基本7色（黒/青/茶/橙/赤/白/黄）をシード決定論でシャッフル→惑星改造ボード7スペース順を
    Setup右カラムに番号付き色表示。独立RNG（planetSatellites）・勢力非依存・基本/LF常時。
    SetupResult.planetSatellites を追加（入力ベースの保存キー/トークンは不変）。
- [x] **G** #9 詳細表クリック→マップにマーカー連動（初版）
  - 実装: MapBoardViewer に markers prop（リング点滅・タイル同一変換・ホバーポップアップ）。
    BreakdownTable の scout/scoutCore/outer/touch/total セル＋列ヘッダ◎をクリック可能化。
    単一選択（再クリック解除）＋Ctrl複数、盤面変更で自動クリア。帰属＋点数＋距離を表示。
  - 要フィードバック/残: ①スカウトの「船別色」未対応（現状は惑星色で統一）。
    ②gaia/cluster は audit に座標付きヒットが無く未対応（必要なら evaluateSoft 側で座標を出す）。
    ③評価指数サマリ行のクリックは詳細表で代替（個別配線は未）。
    ④リングと惑星アートの微小ズレはタイル手動補正由来（既知・据え置き方針）。実機確認で要調整可。

## レビュー待ち

- **種族別重み DRAFT のレビュー**（`src/gaia/eval/factionWeights.ts`）
  一覧タブ「セット提案」で使用中。タイル×種族14の重み（-2..+2）・マップ親和度
  （ガイア/次元横断）・推奨基準式（factionEval.ts）とマップ式（mapFaction.ts）が対象。
  数値・式の差し替えだけで挙動が変わる（テストは機構のみ固定）。

## 検討事項（着手前にユーザー確認）

- 3人時の 09/10 除外縮小盤 `base_3p_small`（要望があれば別テンプレートで）。
- 種族別総合評価タブ（マップ＋setup合算の一覧表示。セット提案の重み確定後）。

## 決定済み（対応しない／確定）

- **新評価軸（ガイア近接・星系クラスタ）の Lost Fleet 反映**: opt-in として実装済み
  （`applyExtraAxesLF`。OFF時はLFキーがbyte不変で既存保存結果と同一バケット）。
- **表示側「見た目のズレ」(旧Phase B)**: 対応なしで確定。評価側ジオメトリは
  uiqoo collision.js とルールブック双方で正当性を確認済み、現行表示も正常。
  グループ別 C_group 統一・viewerAssets 手動補正の除去は見返りゼロ・検証コスト過大のため据え置き。
- **スカウト配置**: 3人=合法5通り／4人=合法21通り（旧14→21）で確定。
  規則生成テスト（placementFromSeed.scoutLegal.test.ts）でドリフト検出。
- **ラウンド得点「学院/首府+5VP が同一2枚」**: 実物確認済みで確定。

## 参考

- マップ素材 map_tile_01..10 / map_deep_* / map_interspace_* は uiqoo に存在
  （未DL・名前判明済み）— 画像ベースのミニ盤面が欲しくなったら利用可。
