# HANDOFF

最終更新: 2026-08-29（本番デプロイ後）。次セッションはここから読む（詳細はTODO.md）。

## 現在地

- **`main` / `origin/main` は `dee9a58`**（2026-08-29 に本番へ出した。`766cc93..dee9a58`
  の6コミット）。**本番（Vercel）は最新**。
- 直近コミット: `dee9a58`（金枠同盟と拡張の基本技術に船ごとの評価軸を追加）。
- ⚠️ **`release/v1.01` は `766cc93` のまま取り残されている**。今回は作業ブランチ
  `claude/gaia-map-project-status-adrt1n` から `:main` へ直接 FF push したため。
  ローカルで `git checkout release/v1.01 && git merge --ff-only origin/main` して
  早送りしておくこと。以後の運用は従来どおり `git push origin release/v1.01:main`。
- 次に本番へ出すときの未pushの件数は
  `git rev-list --count origin/main..release/v1.01`。実行前に必ずユーザー確認。

## 2026-08-29 のセッションでやったこと（本番へ出済み）

1. **`/weights` 編集1079セルを反映**（`695a284`。拡張版のみ）。同盟FED/ART 174 /
   ラウンド得点 430 / 上級技術 475（研究6列と25点・3船）。
   これで **ラウンド得点 × 拡張4種族の雛形が解消**（全ラウンド同値の列が
   48/48→13/48）、**拡張部の面に初めて実値が入った**（21枚中8枚）。
2. **金枠同盟(FEDG1〜8)と拡張の基本技術(TSL1〜3)に「乗せた船」の軸を追加**
   （`dee9a58`。今回の本体）。詳細は下の「船ごとの評価軸」。
3. HANDOFF/TODO の最新化（`41868b5`）、`SETUP_SCORE_DIVISOR` の記述を実装（28）へ
   合わせる（`9c86932`）、`gen_*_table.py` の POSIX 対応（`838ee79`）、
   `package-lock.json` の整合（`63bf8c5`）。

## 船ごとの評価軸（2026-08-29 追加）

- `data/weights/ship_tile_weights_lf.csv` が**船ごとの上書き**（FEDG8×4隻＋TSL3×3隻）。
  **0（空欄）はタイルの基準値へフォールバック**するので、船で差が出るところだけ
  埋めればよく、基準値を直せば触っていない船が自動で追随する。
- FEDG は4隻すべて、TSL は技術スロットのある3隻だけ（トワイライトは
  アーティファクト置き場）。アーティファクトは船で変わらないので軸なし。
- **いまは全セル0＝評価値への影響はゼロ**。値を入れるのはこれから。
- `/weights` の「ブースター他」タブ・拡張版でタイルを選ぶと船の列が出る。
- 生成は `--emit-file` に引数4つ（base / lf / ship）、検算は `--check-ship`。

## それ以前のセッションでやったこと（2026-08-14 〜 08-17）

**ユーザーが `/weights` で採点した値を反映し、拡張版へ流し込む作業の続き。**

1. **`33c2995..8dd19e6`（2026-08-14 に本番push済み）**
   - 差分1139セルを CSV へ反映（`apply_weight_edits.py`）。上級技術400 /
     ラウンド得点620 / ブースター・最終・同盟110 / 標準技術6 / 拡張版の上級技術3。
   - **ラウンド得点の「+4VP 版」を「+3VP 版」の 4/3 で導出**（ユーザー指示。採点したのは
     3点側だけ）。RS02→RS03 / RS05→RS06。`scripts/derive_round_scoring_4vp.py`。
     **3点側を直したら流し直すこと。**
   - **通常版の値を拡張版へ上書き**（2243セル）。`scripts/copy_base_to_lf.py`
     （`/weights` の「全コピー」の CLI 版）。共通14種族 × 両版にある行だけが対象。
     素点が版で違う TS3 は `TILE_VP_LF/TILE_VP`（14/12）を掛けてから書き込む。
   - **拡張部の面（25点/3船）を全セル0へ**。0 なら実行時に6列の最大値へフォールバック
     するので、研究列を直すたび自動で追随して古びない。
   - **`/weights` の拡張版タブで「通常版と同じ値」のセルに地色**（`COPIED_BG` の橙）。
     判定は `sameAsBase`（値そのものの比較）。`scaleToLf` で換算を1箇所へ集約した。
     ついでに `copy_base_to_lf.py` / `rescale_weight_csv.py` の `max(1, ...)` が
     **0 を 1 に化けさせていた**丸めバグを修正。
2. **`0ee37a4`（2026-08-14）**: **RS01（鉱山建設+2VP）のフィラク族・イタル人の R1・R2 を
   0 → 2**（0 は「取りに行けない」の意味なので強すぎた）。**`LF_REVIEW_HINTS` を新設**し、
   拡張の要素で価値が動くはずのタイル11枚に理由付きで印を付けて、拡張版タブでは
   「通常版と同じ値」を2色（橙／桃）に分けた。
3. **`766cc93`（2026-08-17）**: `/weights` 編集703セルを反映。標準技術lf 279 /
   上級 AT15(地球人) 6 / ラウンド RS01・RS03 6 / 同盟・ブースター等 base 71 + lf 341。
   通常版だけ動いた `advanced_tech` / `round_scoring` は `copy_base_to_lf.py` で
   共通14種族へ追随（12セル）。FED系は差分に拡張版の値が明示されていたのでそちらを採用。

各コミットで CSV↔TS の全セル突き合わせ（両方向）が全一致を確認済み。

## 検証状況（2026-08-29・デプロイ時点）

- `npm run typecheck` **0件** / `npm run lint` **エラー0**（warning 587件、ほぼ
  `no-explicit-any`）/ `npm test` **22ファイル293件 全緑** / `npm run build` 成功。
- CSV↔TS の全セル突き合わせ（両方向）**9本すべて「全一致」**
  （4本 × base/lf ＋ 船の上書き `--check-ship`）。
- 影響力（`_probe_category_influence.ts` 4人LF 200件の占有率）:
  技術26.7% > 上級16.3% > ブースター15.4% > LF船15.3% > 最終9.2% > **同盟8.6%** >
  ラウンド6.5% > 追加上級1.9%。1079セルの反映で同盟が 3.1%→8.6% へ上がり、
  ラウンドが 8.0%→6.5% へ下がった。目安の順とは違うが、CLAUDE.md の方針どおり
  係数では合わせ込んでいない。
- 評価値のレンジ（`_probe_score_range.ts` 4人LF 300件）: **117.5〜178.5**、
  盤面ごとの最高値は中央値154。狙いの「100前後」に収まっている。
- **`npm ci` は直した**（`63bf8c5`）。以前は `@emnapi/*` の推移的 optional 依存が
  lock に無く EUSAGE で落ちていた。いまはクリーンインストールが通る。

## 残タスク（優先順）

1. **船ごとの値の入力**（`ship_tile_weights_lf.csv`）。いま全セル0＝どの船でも
   基準値と同じ。FEDG1〜8 × 4隻、TSL1〜3 × 3隻。**差が出るところだけ**入れればよい。
2. **拡張専用の値の残り**。上級技術 AT16〜AT21（6枚）・ラウンド得点 RS10〜RS12（3枚）と、
   拡張4種族（モウェイド人・スペースジャイアント・ティンカーロイド・ダルカニア人）の
   未入力ぶん。**ラウンド得点 × 拡張4種族の雛形は 2026-08-29 に解消済み**
   （全ラウンド同値の列が 48/48 → 13/48。共通14種族の26%と同水準）。
3. **拡張部の面（25点/3船）の残り**。21枚中8枚（AT03/04/06/07/08/09/10/12）に実値が
   入った。残る13枚は全0＝6列最大値へフォールバック中。
4. **狭い画面（560px以下）の「指定」バッジ（緑の丸）の実機確認**。埋め込みブラウザでは
   375px 幅を再現できないので本番での目視が要る。
5. **Map と Setup の合算比**（いまは 1:1 固定。2026-08-04 ユーザー指示で保留・検討中）。
   TODO.md「直近の依頼で未着手」の**「Map と Setup のバランスを決める」**を参照。
6. 勢力ボードの**初期研究レベルが未確認**（ルールブックの別表になく勢力ボードの画像
   にしかない）。実物かボード画像が手に入れば `TECH_POSITION_WEIGHTS` の精度が上がる。

## 環境の注意

- 起動: `npm run dev`（port 3000）。検証は `npm run typecheck` / `npm run lint`
  / `npm test`。依存の導入は `npm ci`（2026-08-29 に lock を直したので通る）。
- **CWD確認を毎回**: `cd /c/work/GaiaProjectMapSearch && pwd`。似た名前の
  `C:\work\GaiaSetupScanner`（別リポジトリ・ローカル専用）があるので取り違えに注意。
- `data/weights/*.csv`が正本、`src/gaia/eval/*.ts`は自動生成物（手で編集しない）。
  手順は`data/weights/README.md`。
- `git push`は指示があるまでしない（`release/v1.01`単独pushは既に許可されているが、
  **`:main`へのFFマージ＝本番デプロイは別途都度確認**）。
