# HANDOFF

最終更新: 2026-08-14。次セッションはここから読む（詳細はTODO.md）。

## 現在地

- ブランチ: `release/v1.01`。未コミット変更なし。
- 直近コミット: `8dd19e6`（拡張版で「通常版と同じ値」に地色）。
- **2026-08-14 に `origin/main` へ push 済み（`33c2995..8dd19e6`）＝本番へ出してある**
  （ユーザー確認のうえ実施）。次に出すときも `git push origin release/v1.01:main`。
  未pushの件数は `git rev-list --count origin/main..release/v1.01`。

## このセッションでやったこと

**ユーザーが `/weights` で採点した通常版の値を反映し、拡張版へ流し込んだ。**

1. 差分1139セルを CSV へ反映（`apply_weight_edits.py`）。
   上級技術400 / ラウンド得点620 / ブースター・最終・同盟110 / 標準技術6 /
   拡張版の上級技術3（バルタック人の航法列を0にするもの）。
2. **ラウンド得点の「+4VP 版」を「+3VP 版」の 4/3 で導出**（ユーザー指示。採点したのは
   3点側だけ）。RS02→RS03 / RS05→RS06。`scripts/derive_round_scoring_4vp.py`（新規）。
   **3点側を直したら流し直すこと。**
3. **通常版の値を拡張版へ上書き**（2243セル。ユーザー指示）。
   `scripts/copy_base_to_lf.py`（新規。`/weights` の「全コピー」の CLI 版）。
   共通14種族 × 両版にある行だけが対象。拡張専用4種族と拡張専用の行は触らない。
   素点が版で違う TS3 は `TILE_VP_LF/TILE_VP`（14/12）を掛けてから書き込む。
4. **拡張部の面（25点/3船）を全セル0へ**。3 で研究列が動いた結果、導入時に置いた
   「6列最大値のコピー」が250セル食い違ったため。0 なら実行時に6列の最大値へ
   フォールバックするので、導入時の仮置きと同じ意味に戻り、以後は自動で追随する。
5. **`/weights` の拡張版タブで「通常版と同じ値」のセルに地色**（`COPIED_BG` の橙）。
   判定は `sameAsBase`（値そのものの比較）なので由来を記録せずに済み、見直して
   値を動かせば自然に外れる。`scaleToLf` で通常版→拡張版の換算を1箇所へ集約した。
   ついでに **丸めのバグを修正**: `copy_base_to_lf.py` / `rescale_weight_csv.py` の
   `max(1, ...)` が **0 を 1 に化けさせていた**（実害は TS3 航法バルタック人の1セル）。

各コミットで CSV↔TS の全セル突き合わせ（両方向）が全一致、
typecheck 0件 / lint エラー0 / test 281件全緑を確認済み。

影響力（`_probe_category_influence.ts` 4人LF 200件の占有率）:
技術33.0% > ブースター20.7% > 上級18.5% > LF船8.9% > ラウンド7.9% > 最終7.2% >
同盟2.4% > 追加上級1.4%。目安の順とはブースターと LF船 の位置が違うが、
CLAUDE.md の方針どおり係数では合わせ込んでいない。

## 残タスク（優先順）

1. **origin/main への反映をどうするか、ユーザーに確認**（上記「現在地」参照）。
   今回の変更は評価値が広く動くので、出すなら実データでの見え方を先に確認したい。
2. **拡張専用の値がまだ雛形**。拡張4種族（モウェイド人・スペースジャイアント・
   ティンカーロイド・ダルカニア人）の全表と、拡張専用タイル
   （上級技術6枚・ラウンド得点3枚・ブースター等31枚）。
3. **拡張部の面（25点/3船）の値**。いまは0＝6列最大値へフォールバック。
   面ごとの差を入れるならここ。
4. Map と Setup の合算比（いまは 1:1 固定）。TODO.md の「判断待ち」参照。

## 環境の注意

- 起動: `npm run dev`（port 3000）。検証は `npm run typecheck` / `npm run lint`
  / `npm test`。
- **CWD確認を毎回**: `cd /c/work/GaiaProjectMapSearch && pwd`。似た名前の
  `C:\work\GaiaSetupScanner`（別リポジトリ・ローカル専用）があるので取り違えに注意。
- `data/weights/*.csv`が正本、`src/gaia/eval/*.ts`は自動生成物（手で編集しない）。
  手順は`data/weights/README.md`。
- `git push`は指示があるまでしない（`release/v1.01`単独pushは既に許可されているが、
  **`:main`へのFFマージ＝本番デプロイは別途都度確認**）。
