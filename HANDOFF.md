# HANDOFF

最終更新: 2026-09-26（引継準備。コードは 2026-09-20 の9回目の FF push から変えていない）。次セッションはここから読む（詳細はTODO.md）。

## 現在地

- **`main` ＝ `release/v1.01`（2026-09-20 の9回目の FF push で揃えた。ハッシュは
  `git log -1 origin/main` で見る）。作業ブランチ `claude/gaia-map-project-status-adrt1n`
  はその上に記録コミット（ドキュメントのみ。9回目の記録＋この引継準備）が乗っていて、
  draft PR #11 として開いてある。次に本番へ出すときに一緒に FF push する**
  （アプリの動きは `main` と同じなので急がない）。**本番（Vercel）は最新**。
  9回目は `7c86d33..fbf2fa4` の4コミット（List の合計上位を人数+2色に `b46c75e`・
  保存リストのチップ折り返し `5b181dc`・「画面幅に収める」の余白 `ac37f6f`・docs
  `fbf2fa4`）。8回目は `4db4f3b..` の3コミット（レビュー資料 `d21e852`・`/weights` の
  レビュー採否 `9add777`・その記録）。7回目（2026-09-19）は初期研究レベルの保存・
  合算比の入力。いずれも重みは変えていないので `/weights` の「全消去」は不要。
  重みを変えた直近のデプロイは 4回目 `5e3771a..06cfc4f`（船ごとの値の初回320セル）と
  5回目 `06cfc4f..2c487d8`（船セルの見せ方 `996476b`）。
- **評価値の入力は 2026-09-19 に全表で一巡完了**（ユーザー確認）。`/weights` の編集は
  すべて CSV へ反映済みなので、**localStorage は「全消去」してよい**（「確認済み」の
  チェックは別キーなので残る）。以後の値の直しは README（data/weights）の手順どおり
  `/weights` → 差分 → `apply_weight_edits.py` → 生成 → 検算。
  `9c33187` はユーザーが GitHub の Web UI から `public/` 直下へ転送用の5ファイル
  （note の船画像4枚と `gaia lf rule.pdf`）を直接アップロードしたコミット。
  **この5ファイルはユーザー判断でそのまま残す**（本番サイト直下で配信されている）。
- 重みの直近: `06cfc4f`（船ごとの値の初回320セル）。船セルの見せ方の変更 `996476b` は
  CSV の見た目（基準値と同値の上書き101セル→0）を変えるだけで実行時の値は不変。
- **`release/v1.01` は 2026-09-19（2回目）に `origin/main` と同じ位置へ早送りした**
  （origin 側も push 済み）。それまでは 8/29・9/13・9/19 の計6回とも作業ブランチから
  `:main` へ直接 FF push していて `766cc93` に取り残されていた。手元の PC のローカル
  `release/v1.01` は `git checkout release/v1.01 && git pull --ff-only` で揃う。
  次に本番へ出すときは release/v1.01 を作業ブランチまで早送りしてから従来どおり
  `git push origin release/v1.01:main`（実行前に必ずユーザー確認）。未pushの件数は
  `git rev-list --count origin/main..release/v1.01`。

## 2026-09-20（2回目）のセッションでやったこと（本番へ出済み）

ユーザーの実データ確認（Total/List・レビュー画面・Android 実機）への対応。9回目の FF push
（`7c86d33..fbf2fa4`、4コミット）。重みは変えていない。

1. **List の「合計の上位」を人数+2色にした**（`b46c75e`）。同じ色の2種族は卓で1人しか
   選べないので、色ごとに強い方の種族だけを合計の順に出す（`topFactionsByColor`）。
2. **保存リストの種族チップを折り返す**（`5b181dc`）。Android でチップが行からはみ出していた。
3. **「画面幅に収める」で余白と画像も縮小率に合わせて縮める**（`ac37f6f`）。列幅だけ縮めて
   余白が固定だったので 412px で画像が 39px しか取れなかった → 52px。寸法は container
   query 単位（cqw）で CSS だけで決める（`.setup-tracks-fit`、`@supports` で囲む）。
4. **Map を3倍にする案は検証だけ**（下の「Map を3倍にする案の検証」。ユーザー指示で実装せず）。
5. TODO の整理: 初期研究レベルの項を完了扱い、LF船×スカウトを実装待ちへ、
   `SETUP_WEIGHT_MIN/MAX` の項を解消、`criterionScore` の 0.2/0.3 の説明を追記。
   「反映済み」の印は付けない（ユーザー判断）。

## 2026-09-20 のセッションでやったこと（本番へ出済み）

1. **整合性レビューの提案を `/weights` で読み、採否できるようにした**（ユーザー確定:
   採用で値が編集に入り差分に出る／タイルごとの表示＋上部に一覧）。
   - `scripts/gen_weight_review.py` → `src/gaia/eval/weightReview.ts`（自動生成。
     `docs/weights-review-2026-09-19.md` と `.edits.txt` から提案30・要判断13・セル443）。
   - `src/gaia/eval/weightReviewEdits.ts`: 採否の反映。採用＝軸なしの行は `edits.base`、
     軸のある行は**新設の `edits.value`（値そのもの。倍率より優先）**。見送り＝その提案の
     値と同じときだけ外す（手で直した値は残す）。`itemsForTile` / `locateTile` / `reviewNotes`。
   - `src/components/WeightReviewPanel.tsx`（一覧とカード）を `WeightsEditor` に組み込み:
     ヘッダーの「レビュー 未定n」、タイルのカード、根拠のタイルへ飛ぶボタンと「← 戻る」、
     提案が触るセルは点線の枠、選択バーに「レビュー採用の値」（外すボタン）。
   - 対象のタイルが無い要判断（L06〜L08）は一覧の中で開く。採否は localStorage の
     別キー `gaia_weight_review`（全消去では消えない）。差分の末尾に `# review Pxx = adopt`。
   - `WeightEdits.value` を追加（保存データは無ければ `{}`。既存キーは不変）。
2. 検証: typecheck 0 / lint エラー0（warning 586）/ test 25ファイル311件。ヘッドレス
   Chromium（375px）で 一覧 → P30 → RS12 → 採用（差分に `RS12,R1,firaks,4` と記録行）→
   見送り（外れる）→ 戻る → 再読込で採否が残る、を確認。

## 2026-09-19（2回目）のセッションでやったこと（本番へ出済み）

1. **`release/v1.01` を `origin/main` へ早送り**（origin へも push。本番には触っていない）。
2. **狭い画面の「指定」バッジを 375px で確認した**（残タスク1）。この環境から本番は
   403 で届かないので、同じコミットのローカル dev サーバをヘッドレス Chromium
   （Playwright、viewport 375px）で撮った。列幅 51.8px・画像 33.8px・バッジは緑の丸
   8px＋白縁 1px＝10px（画像幅の 30%）で枠内に収まり、上級・標準の段は6列とも上端が
   一致、横スクロールなし。560px で丸・561px で文字「指定」に切り替わる。拡張版でも同じ。
   残るのは Android のフォントで見出しの折り返し行数が変わる可能性だけ（段揃えは grid
   なので崩れない）。既定の除外だけの枠（テラフォーミング／ガイア／経済の上級）にも
   バッジが出るのは `hasRule` の仕様どおりで、変えていない。
3. **勢力ボードの初期研究レベルを保存した**（残タスク3。ユーザーが実物で確認した値）。
   `FACTION_START_RESEARCH`（`src/gaia/eval/factionWeights.ts`、テストで固定）と
   `data/weights/README.md` の表。ガイア: terrans/balTaks/moweyds、改造: geodens、
   航行: gleens/ambas/spaceGiants/darkanians、経済: hadschHallas/darkanians、
   科学: nevlas/tinkerroids、AI: xenos、なし: lantids/taklons/ivits/bescods/itars/firaks。
   評価には直接掛けていない（研究列の値を見直すときの根拠）。
4. **Map と Setup の合算比を Total タブで指定できるようにした**（残タスク2、案C）。
   `src/gaia/eval/scoreBlend.ts`（既定 1:1、localStorage `gaia_score_blend` に既定と
   違うフィールドだけ）＋ `useScoreBlend`。Total の合計列、List の種族優遇の掛け先
   （`factionPrefBonus` の `blend`）、List の「合計の上位4種族」が同じ比を使う。
   1:1 のままなら保存キーは無く、探索結果・条件キーはバイト不変。**既定値をどうするかは
   未決**（実データで試してから）。
5. 検証: typecheck 0 / lint エラー0（warning 586。`ResearchTrackId` の未使用 import が
   使われるようになって1件減った）/ test 24ファイル303件。Total と List はヘッドレス
   Chromium で入力→保存→再読込→List の表示→1:1 に戻すまで動作確認済み。
6. **評価値の整合性レビュー**（ユーザー依頼。値は変えていない）。9本の CSV 10,064 セルを
   導出則・研究列の係数・行内外れ値・通常/拡張差・ラウンド曲線・種族の平均比・似たタイルの
   並べ比べ・下書き相性との食い違いで精査し、`docs/weights-review-2026-09-19.md` に
   提案30件（高7・中23）＋要判断13件を表にした。高・中の提案は同名の `.edits.txt`
   （/weights の差分形式、443セル）にしてあり、`apply_weight_edits.py --dry-run` で
   通ることを確認済み。**採否はユーザー**。採るなら README（data/weights）の手順で
   反映→生成→検算→影響力の測り直し。

## 2026-09-19 のセッションでやったこと

1. **`/weights` 編集320セルを反映**（`06cfc4f`。4回目の本番デプロイで出済み）。
   基準値 160（TSL1/TSL3、FEDG2/5/7/8、ART09〜13）＋**船ごとの値 160（初めて非ゼロ）**。
   影響力は 技術 26.1% > LF船 17.6% > 上級 15.2% > ブースター 15.1% > 最終 9.0% >
   同盟 8.4% > ラウンド 6.4% > 追加上級 2.3%。LF船が 15.4%→17.6% で目安の順の上位4つに揃った。
2. **上書きの無い船セルを基準値として表示し、差分は上書きだけにした**（`996476b`。
   5回目の本番デプロイで出済み）。
   1の差分は「基準値と同じにするためだけの 100%」が101セルあったため。
   `effectiveStoredValue` を追加し、`finalValueOf` は上書きの無い船セルを編集後の基準値へ
   追随させ、`collectDiffs` の船の行は基準値と違うところだけ（上書きを戻すと 0）。
   CSV 側も基準値と同値の上書き101セルを 0 に戻した（実行時の値は738セル突き合わせで差0。
   残る上書き59セル＝FEDG2 29 / TSL3 T.F.マーズ 18 / TSL2 12）。
3. 換算の目安を出した（標準技術の表から逆算）: 1VP=1点、クレジット1≈1.4、鉱石1≈3、
   知識1≈2.6、チャージ1≈1〜1.3。即時のまとまりは 1.3〜1.5倍。
   「即時2クレ＋鉱石3」≈16、「即時鉱石1＋知識3」≈15。船の技術タイル枠のおまけは
   T.F.マーズ チャージ2 / エクリプス チャージ3＋知識2 / リベリオン チャージ3＋鉱石1（画像から読んだ値）。
4. **評価値の入力が一巡完了したことを確認した**（ユーザー判断）。CSV を走査して
   「雛形のまま」に見えた3点は、いずれも**意図どおりの値**と確認済み。次のセッションで
   再び未入力扱いにしないこと:
   - RS10〜RS12（拡張のラウンド得点）は18種族とも R1〜R6 が同値（6/6/4）。ラウンド差なし
     で確定。
   - `LF_REVIEW_HINTS` の11枚のうち RS08・AT02・RB02 は全セルが通常版と同値、
     AT12・AT01・AT06 もほぼ同値。見直したうえで同じ値、が結論。
   - 船ごとの上書きがあるのは FEDG2・TSL2・TSL3 だけ（59セル）。FEDG1・3〜8 と TSL1 は
     どの船でも基準値。
   残る「通常版と同じ値」の橙マーカー（上級 90% / ラウンド 88% / 標準 75% / ブースター他
   93%）は**見直し済みで同値**なので、色が付いていても未見直しではない。

## 2026-09-13（2回目）のセッションでやったこと（本番へ出済み）

1. **`/weights` に船ボードの画像を表示**（`f9b2074`）。拡張版「ブースター他」で列が
   船のとき（一括、または FEDG/TSL のタイル）、グリッドの上に船のサムネイルを並べる。
   選択中のセルの船は青く、タップで拡大、もう一度で閉じる。「原寸で開く」で別タブ。
   画像が無ければ名前だけの枠。評価値は一切変えていない。
2. **船ボードの画像4枚**（`f4b76dc`、その後差し替え）。最初は Lost Fleet ルールブック
   （英語版 PDF）7ページの図を切り出した（各 709×225）が、**実機で画質が悪い**との
   ことで、`public/` 直下にアップロードされた note の画像4枚をそのまま `public/ships/`
   へ複製した（twilight.png 526×162 / eclipse.png 700×218 / rebellion.jpg 700×218 /
   tfmars.jpg 1200×360、計約360KB）。形式が船ごとに違うので参照は
   `WeightsEditor.tsx` の `SHIP_IMG_SRC` で持つ。
3. 1回目のリリース記録（`047c20e`）。
4. 上記2の差し替えは **3回目の本番デプロイ**（`f4b76dc..5e3771a`）で本番へ出した。
5. **材料の受け渡しで分かったこと**: この作業環境は Google と note のホストへ出られない
   （通信ポリシーの 403）。Drive コネクタも中身を会話へ流す方式で 3MB は通らない。
   **GitHub 経由（ブランチへ push、または Web UI でアップロード）だけが確実**。

## 2026-09-13（1回目）のセッションでやったこと（本番へ出済み）

1. **`/weights` 編集1229セルを反映**（`c9a887d`。`advanced_tech_lf.csv` のみ）。
   AT13/01/05/15/02/11/14 の研究6列と25点・3船、拡張専用 AT16〜AT21 の全部。
   これで **拡張部の面（25点/3船）が 21/21**、**AT16〜AT21 も研究6列まで埋まった**。
2. 上記に伴う HANDOFF/TODO/README の更新（`149fb99`）と、8/29 のリリース記録（`9d5612a`）。

## 2026-08-29 のセッションでやったこと（本番へ出済み）

1. **`/weights` 編集1079セルを反映**（`695a284`。拡張版のみ）。同盟FED/ART 174 /
   ラウンド得点 430 / 上級技術 475（研究6列と25点・3船）。
   これで **ラウンド得点 × 拡張4種族の雛形が解消**（全ラウンド同値の列が
   48/48→13/48）、**拡張部の面に初めて実値が入った**（21枚中8枚）。
2. **金枠同盟(FEDG1〜8)と拡張の基本技術(TSL1〜3)に「乗せた船」の軸を追加**
   （`dee9a58`。今回の本体）。詳細は下の「船ごとの評価軸」。
3. HANDOFF/TODO の最新化（`41868b5`）、`SETUP_SCORE_DIVISOR` の記述を実装（28）へ
   合わせる（`9c86932`）、`gen_*_table.py` の POSIX 対応（`838ee79`）、
   `package-lock.json` の整合（`63bf8c5`）。

## 船ごとの評価軸（2026-08-29 追加）

- `data/weights/ship_tile_weights_lf.csv` が**船ごとの上書き**（FEDG8×4隻＋TSL3×3隻）。
  **0（空欄）はタイルの基準値へフォールバック**するので、船で差が出るところだけ
  埋めればよく、基準値を直せば触っていない船が自動で追随する。
- FEDG は4隻すべて、TSL は技術スロットのある3隻だけ（トワイライトは
  アーティファクト置き場）。アーティファクトは船で変わらないので軸なし。
- 上書きは **59セル**（2026-09-19。FEDG2 29 / TSL3 T.F.マーズ 18 / TSL2 12）。
  FEDG1・3〜8 と TSL1 はどの船でも基準値（意図どおり、ユーザー確認済み）。
  `/weights` では上書きの無い船セルは基準値がそのまま出て、差分に出るのは上書きだけ。
- `/weights` の「ブースター他」タブ・拡張版でタイルを選ぶと船の列が出る。
- 生成は `--emit-file` に引数4つ（base / lf / ship）、検算は `--check-ship`。

## それ以前のセッションでやったこと（2026-08-14 〜 08-17）

**ユーザーが `/weights` で採点した値を反映し、拡張版へ流し込む作業の続き。**

1. **`33c2995..8dd19e6`（2026-08-14 に本番push済み）**
   - 差分1139セルを CSV へ反映（`apply_weight_edits.py`）。上級技術400 /
     ラウンド得点620 / ブースター・最終・同盟110 / 標準技術6 / 拡張版の上級技術3。
   - **ラウンド得点の「+4VP 版」を「+3VP 版」の 4/3 で導出**（ユーザー指示。採点したのは
     3点側だけ）。RS02→RS03 / RS05→RS06。`scripts/derive_round_scoring_4vp.py`。
     **3点側を直したら流し直すこと。**
   - **通常版の値を拡張版へ上書き**（2243セル）。`scripts/copy_base_to_lf.py`
     （`/weights` の「全コピー」の CLI 版）。共通14種族 × 両版にある行だけが対象。
     素点が版で違う TS3 は `TILE_VP_LF/TILE_VP`（14/12）を掛けてから書き込む。
   - **拡張部の面（25点/3船）を全セル0へ**。0 なら実行時に6列の最大値へフォールバック
     するので、研究列を直すたび自動で追随して古びない。
   - **`/weights` の拡張版タブで「通常版と同じ値」のセルに地色**（`COPIED_BG` の橙）。
     判定は `sameAsBase`（値そのものの比較）。`scaleToLf` で換算を1箇所へ集約した。
     ついでに `copy_base_to_lf.py` / `rescale_weight_csv.py` の `max(1, ...)` が
     **0 を 1 に化けさせていた**丸めバグを修正。
2. **`0ee37a4`（2026-08-14）**: **RS01（鉱山建設+2VP）のフィラク族・イタル人の R1・R2 を
   0 → 2**（0 は「取りに行けない」の意味なので強すぎた）。**`LF_REVIEW_HINTS` を新設**し、
   拡張の要素で価値が動くはずのタイル11枚に理由付きで印を付けて、拡張版タブでは
   「通常版と同じ値」を2色（橙／桃）に分けた。
3. **`766cc93`（2026-08-17）**: `/weights` 編集703セルを反映。標準技術lf 279 /
   上級 AT15(地球人) 6 / ラウンド RS01・RS03 6 / 同盟・ブースター等 base 71 + lf 341。
   通常版だけ動いた `advanced_tech` / `round_scoring` は `copy_base_to_lf.py` で
   共通14種族へ追随（12セル）。FED系は差分に拡張版の値が明示されていたのでそちらを採用。

各コミットで CSV↔TS の全セル突き合わせ（両方向）が全一致を確認済み。

## 検証状況（2026-09-20）

- `npm run typecheck` **0件** / `npm run lint` **エラー0**（warning 586件、ほぼ
  `no-explicit-any`）/ `npm test` **25ファイル312件 全緑**（2026-09-20 2回目）。
- CSV↔TS の全セル突き合わせ（両方向）**9本すべて「全一致」**
  （4本 × base/lf ＋ 船の上書き `--check-ship`。船の上書きは59セル）。
- 影響力（`_probe_category_influence.ts` 4人LF 200件の占有率）:
  技術26.1% > **LF船17.6%** > 上級15.2% > ブースター15.1% > 最終9.0% > 同盟8.4% >
  ラウンド6.4% > 追加上級2.3%。船ごとの値が入って LF船が 15.4%→17.6% に上がり、
  目安の順の上位4つ（技術 > LF船 > 上級 > ブースター）に揃った。下位は目安と違うが、
  CLAUDE.md の方針どおり係数では合わせ込んでいない。
- 評価値のレンジ（`_probe_score_range.ts` 4人LF 300件）: **116.2〜176.9**、
  盤面ごとの最高値は中央値154。狙いの「100前後」に収まっている。
- **`npm ci` は直した**（`63bf8c5`）。以前は `@emnapi/*` の推移的 optional 依存が
  lock に無く EUSAGE で落ちていた。いまはクリーンインストールが通る。

## 残タスク（優先順）

**評価値の入力（4表 × 通常/拡張 ＋ 船の上書き）は 2026-09-19 に一巡完了**。
2回目のセッションで「指定」バッジの確認・初期研究レベルの保存・合算比の入力まで済んだ。
Total/List の実データ確認と Android 実機の確認は 2026-09-20 に済み、指摘（List の
「合計の上位」を人数+2色に／保存リストのチップのはみ出し／「画面幅に収める」の
余白）は同日に直した（`b46c75e` `5b181dc` `ac37f6f`）。

1. **合算比の既定値を決める**（いまは 1:1）。ユーザーの方向は「Map の最上位が 100〜150
   になるのが望ましい（手元では現状の2〜3倍）」。何倍にするかは未決で、**実装はしない**
   （ユーザー指示）。検討材料は下の「Map を3倍にする案の検証」—— 既定の評価指数だと
   基本版の Map 最上位は 55、LF は 135〜149 なので、一律3倍は LF で Map が Setup の
   3倍の桁になる。決まったら `DEFAULT_SCORE_BLEND` を変える（保存値は絶対値なので
   既定を変えても保存済みの比はそのまま）。
2. **整合性レビューの提案の採否**（`docs/weights-review-2026-09-19.md`）。画面は
   問題なし（2026-09-20 ユーザー確認）。要判断 L01〜L13 はユーザー確認中。`/weights` の
   「レビュー」から提案ごとに採用／見送りを押し、「差分を出す」で普段どおり差分を送る
   （PC 側は `apply_weight_edits.py` で反映するだけ）。`.edits.txt` を直接流す道も
   残っている（採らない行を消して渡す）。「反映済み」の印は付けない（ユーザー判断）。
3. **LF船の中身とスカウトの位置の相互作用を実装する**（2026-09-20 ユーザー指示で
   棚上げ → 実装待ち）。設計は TODO.md の同名の項目（段階1: List の採点時に
   `mapValueByFaction` へ「船に接触している惑星の評価値 × その船に乗るタイルの種族別
   価値」を足す）。Map の桁（上の1）が決まってから着手するのが安全。
4. （任意）値を実プレイで使ってみて気になったところを `/weights` から直す。
   手順は README（data/weights）。直したら影響力を測り直す（`_probe_category_influence.ts`）。

## Map を3倍にする案の検証（2026-09-20、実装はしていない）

ユーザーの問い: 「Map 側の数値を3倍にして 1:1 で合算する場合に問題はある？」。

- **合計の順位そのものには問題ない**。合計 ＝ map×Map ＋ setup×Setup は線形なので、
  「Map を3倍して 1:1」と「合算比 3:1」は同じ数字・同じ順位。Total タブの合算比に
  Map ×3 と入れれば今日から試せる（上限 `SCORE_BLEND_MAX` = 5）。
- **桁の実測（既定の評価指数、4人、`_probe_score_range.ts` 200件 ＋ Map は
  `_probe_extra_best_bias.ts` と同じ作りで 60盤面）**:

  | | Map 最上位（盤面ごとの中央値） | Setup 最上位（同） |
  |---|---|---|
  | 基本版 | **55**（10〜90%: 47〜63） | 109 |
  | LF 4人 | **149**（133〜180） | 163 |
  | LF 3人 | 135（118〜170） | — |

  基本版の Map が小さいのは船の軸（wScout 10 / wScoutCore 4）が無いため。
  「現状の2〜3倍で 100〜150」は基本版の値（55）にぴったり合う。LF はすでに
  100〜150 の中に入っているので、**一律3倍にすると LF で Map ≈ 450 vs Setup ≈ 163
  になり、Map がほぼ全てを決めてしまう**。手元で LF なのに 40〜60 に見えているなら、
  プロファイルの評価指数が既定より小さい（Map タブの内訳表の「評価」列で確認できる）。
- **「3倍」の入れ方で副作用が違う**:
  - 合算比（3:1）だけ: 副作用は List の種族優遇の目盛りだけ。`LIST_FACTION_PREF_W` は
    「掛け先 ≒ Setup 単体の2倍」を前提に `SETUP_COLOR_PREF_W / 2` にしてあるので、
    掛け先が 3Map＋Setup になると pref=1 の効きが約2倍強くなる（係数を測り直すか
    目盛りの説明を変える）。Map タブ・Total の Map 列の表示は元の桁のまま。
  - Map の評価指数（wOuter/wTouch/wScout…）を全部3倍: 検索の順位は不変（偏り項も
    色優遇項も totals に線形で、同じ倍率で動く）。Map タブの表示が3倍になるので
    「最上位 100〜150」はこちらでしか実現しない。ただし
    (1) 条件プロファイルのキーに評価指数が入るので別プロファイル扱いになる、
    (2) **保存済み候補の内訳（`evaluation.breakdown`）は検索時の評価指数で固定**なので、
    Total/List が読む Map 評価値は古い候補だけ旧桁のまま（再検索かピン留めの作り直しが
    要る）、(3) 種族優遇の目盛りは上と同じ。`EXTRA_BEST_FACTOR`（PROTO/ASTEROID）は
    最良1惑星に掛けるので一緒に3倍になり問題ない。推奨基準（mapTop3 / mapTopK）は
    惑星の個数（`mapFactionScores`）を見るので影響なし。
- **筋のよい形**: 基本版だけ上げる。候補は (a) 合算比の既定値を拡張ごとに持つ
  （基本版 Map ×2、LF ×1 など。`DEFAULT_SCORE_BLEND` を mode 別にする）、
  (b) 基本版の評価指数の既定値だけ上げる（ただし上の (1)(2) の副作用が付く）。
  どちらにするかはユーザー判断（まだ決めない）。

## 環境の注意

- 起動: `npm run dev`（port 3000）。検証は `npm run typecheck` / `npm run lint`
  / `npm test`。依存の導入は `npm ci`（2026-08-29 に lock を直したので通る）。
- **CWD確認を毎回**: `cd /c/work/GaiaProjectMapSearch && pwd`。似た名前の
  `C:\work\GaiaSetupScanner`（別リポジトリ・ローカル専用）があるので取り違えに注意。
- `data/weights/*.csv`が正本、`src/gaia/eval/*.ts`は自動生成物（手で編集しない）。
  手順は`data/weights/README.md`。
- `git push`は指示があるまでしない（`release/v1.01`単独pushは既に許可されているが、
  **`:main`へのFFマージ＝本番デプロイは別途都度確認**）。
- **クラウドの作業環境から本番（vercel.app）へは 403 で届かない**。狭い幅の見た目は
  ローカル dev サーバ＋ヘッドレス Chromium（`/tmp` に `npm i playwright`、実行ファイルは
  `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`、viewport を 375 に固定）で
  同じコミットを撮れば代用できる。Setup にタイル指定を入れるには `/setup?s=<token>`
  （`encodeSetupToken` 相当＝入力 JSON の base64url）で `tileRules` ごと渡すのが速い。
